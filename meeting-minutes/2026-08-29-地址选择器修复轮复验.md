# 地址选择器修复轮 真机复验报告

- 测试人：郑稳（测试/QA，Android 真机自动化）
- 日期：2026-08-29（设备时钟 CST 凌晨；对应服务端 08-28 18:4x~19:06Z）
- 设备：MI 9 SE (ee9999eb)，com.ymm.boss.user versionCode=2，**lastUpdateTime=08-29 02:36:35**（修复轮新包装机确认，含新文案「09 开头共 11 位」DEX 检索 ×1）
- 后端：102:28080，license 持久化生效，读接口全 200（403 回归已解除，实测证实）
- 结论索引：**R1 四项 FAIL 全部修复 ✓，R2 四项 BLOCKED 全部解除 ✓，R3/R4 专项 ✓，R5 抽查无回归 → 可发布**

---

## R1 前轮 FAIL 复验

### A3 系统返回键 — **PASS**
| 场景 | 操作 | 实际 | 判定 |
|---|---|---|---|
| 层1 返回 | L1 按 BACK | 弹层关闭，回表单 | ✓ |
| 层2 返回 | L2 按 BACK | 回 L1（标题「选择大区 / 市」），弹层保持 | ✓ |
| 层3 返回 | L3 按 BACK | 回 L2（标题「选择城市 / 市镇」），弹层保持 | ✓ |
| 遮罩点击 | L2 点弹层外 (540,300) | 回 L1，弹层保持 | ✓ |
| 下滑手势 | L2 列表区下拉 | 回 L1，弹层保持 | ✓ |

**一致性**：层2+ 所有"关闭意图"（BACK/遮罩/手势）统一重映射为回上一级，仅层1 真正关闭；且复验过程中一次误操作的下拉同样被识别为回退（列表顶部继续下拉亦如此），行为完全可预测。

**QA 评价：可接受**。理由：①级联是多级状态容器，误触遮罩丢掉整棵下钻路径的代价 > 多按一次返回；②三种关闭路径语义统一，消除"ModalBottomSheet 遮罩点击 vs 系统返回行为不一致"的经典缺陷；③放弃选择的最坏成本 = 按 N 次 BACK（N≤当前层级深度），且已选回填不丢，无数据损失。提示：与 Material 惯例（遮罩=关闭）不同，用户有学习成本，建议保留现状并在用户反馈中观察。

### B2 面包屑 chip 跳层 — **PASS**
- 修复实证：chip 节点（TextView）`clickable=true`（前轮 false——安栋所述"幽灵行"与实测吻合，semantics 优化生效）。
- 包裹节点 bounds 断言（tap 均使用当轮 dump 的最新 bounds 中心）：
  - L3 点「国家首都区」chip (178,626) → 跳 L2 ✓
  - **L4 点根 chip (178,978) → 直达 L2**（非逐级回退）→ 证实语义 =「跳到该 chip 节点的子层」✓
- 判定说明：L2 点根 chip、L3 点末级 chip 观察到"无变化"，按上述语义为**正确行为**（目标层=当前层），非缺陷。

### B3 加载/错误态点击穿透 — **PASS**
- 断网（`Active default network: none`）后进入未加载城市 → L2 呈错误态：`地区加载失败，网络连接失败，请检查网络` + `重试`。
- 错误态连点原列表区 ×3 → 弹层**未关闭、未回退**，标题与错误态保持不变（前轮穿透误关已修复）。
- 恢复网络（network 104）→ 点重试 → 城市列表完整加载 ✓。测后网络已恢复并复核 ✓。

### D4 手机号（本次后端新语义配套） — **PASS**
| 步骤 | 证据 | 判定 |
|---|---|---|
| 编辑 id11，电话注入 `09171234567`（数字键码） | 保存按钮像素断言 **ENABLED**（主色 8,108,245 deep=1983） | ✓ |
| 保存 | `PUT /api/user/v1/addresses/11 → 200`；库 `phone=09171234567` | ✓ |
| 非法输入 `123` | 表单出现文案 **`手机号格式：09 开头共 11 位`**（口径与设计一致）；按钮像素 **DISABLED**（淡蓝 pale=2107） | ✓ |

上轮"输入电话即静默禁用保存"已修复，且有明确格式文案，符合失败可观测红线。

## R2 前轮 BLOCKED 复验

### B1 返回上级后 filter 清空 — **PASS**
Manila L3 过滤 `6` → 列表仅剩 `Barangay 6` → 点根 chip 跳回 L2 → 重进 Manila L3 → 过滤框为空（占位文案）且列表从 Barangay 1 完整开始，**无残留** ✓。

### B6 手填小区名→选地区→不被覆盖 — **PASS**
先手填 `ZZManual` → cascade 选 Barangay 3 → 区域行回填 `国家首都区 · City of Manila · Barangay 3` + `已绑定层级路径`，**小区名保持 ZZManual 未被覆盖** ✓（改动点⑦）。
测试过程备注：小区名字段 focus 时会弹出**历史小区名联想浮层**（HOME/Commonwealth/8号楼…），点击浮层项会覆盖输入——自动化需先 BACK 收起再输入；真实用户快速输入时也可能误触，建议关注反馈（非阻塞）。

### B8-a 订单确认页选地址 — **PASS**
办套餐 → 测试套餐-500M畅享 → 立即办理 → 确认订单页：`套餐详情 测试套餐-500M畅享 ¥199/月` 加载成功（403 已解除），`选择安装地址` 列表完整（含默认/备用标签）。点击 Commonwealth 卡 → 卡片右上角出现蓝色选中指示（像素断言：仅该卡 blue=45，其余卡 0）。**未支付**（按纪律）。

### B9 阶段3 留空保存保持原值 — **PASS**
09171234567 保存后，再次编辑（电话字段空显）→ 留空 → 保存 → `PUT /addresses/11 → 200`，**库中 phone 保持 `09171234567` 不变**（未被账户号覆盖）✓。新语义「update 空 phone=保持原值」闭环成立。

## R3 空列表归因复验 — **PASS**
`am force-stop` 冷启动 → 点忽略更新弹窗 → 首页 → 我的 → 家庭地址：**进页 2 秒内列表完整渲染**（Sunrise Village 卡+默认标签），无任何下拉/刷新/前置动作 ✓。上轮"进入即空列表"未再复现。

## R4 旧路径格式地址透传 — **PASS**
选择 id6（`address_path=ncr.manila.barangay_6`，编辑态区域行显示裸段 `barangay_6`+`点此重试`）→ 仅楼栋字段填 `R4BRv` → 保存 `PUT /addresses/6 → 200` → 查库：`detail='B3验证小区 R4BRv'`（楼栋正常并入），**`address_path='ncr.manila.barangay_6'` 原样透传，未丢、未清空、未被新树路径改写** ✓。已将楼栋清空回存还原（detail 复原为 `B3验证小区  `，含原尾随空格）。

## R5 前轮 PASS 项抽查 — **全部无回归**
- **A2** 级联主流程：新建地址（ZZManual/Barangay 3）→ 回填+标记+保存出卡（id=11）→ 库 phone=13900001234（阶段1 语义再次验证）✓
- **A4** 搜索：`bara` → 结果带祖先链（`国家首都区 · City of Caloocan`）→ 点叶 `Barangay 1` 直接选中回填 ✓
- **A5** 编辑回显：id11 编辑态区域行 `国家首都区 · City of Manila · Barangay 3` + 标记 ✓
- **B7** 卡片副行：id11 卡 `City of Manila · Barangay 3` ✓；联系人掩码 `091****4567` 佐证显式电话已存 ✓

## 新增观察项（均非阻塞）
1. 编辑表单**不回显已存的显式电话**（placeholder 固定为"留空则使用账户手机号"）。语义自洽（留空=保持），但用户无法看到自己存过的号码，建议回显为占位提示（如"当前：091****4567"）。
2. 小区名**历史联想浮层**见 B6 备注。
3. 「我的」页地址徽标计数不联动（显示"0个地址"，实际 6 条）。
4. 冷启动仍推 `发现新版本 v0.1.0（版本4）` 更新弹窗（装机 versionCode=2）——发布时注意服务端版本表与实际包号对齐。
5. 新增表单首次触碰联系人字段触发系统定位权限弹窗（"使用当前位置"能力），授权一次后不再弹。

## 清理与留痕
- 测试数据：id11（ZZManual）已经 UI 删除并确认弹窗确认；DB 复核仅剩既往 6 条（id1~7），id6 已还原原状。
- 网络：`svc data enable && svc wifi enable` 已执行，`Active default network: 104` ✓。
- App 已退后台（Launcher 聚焦）✓。

## 总结论

**可发布。**

前轮 4 项 FAIL（A3/B2/B3/D4-保存禁用）全部修复并逐条实证通过；4 项 BLOCKED（B1/B6/B8-a/B9 阶段3）全部解除并 PASS；R3 空列表已修（冷启动直进即出）；R4 旧路径透传无损；抽查项无回归。遗留 5 条均为非阻塞观察项，建议纳入下个迭代的体验优化清单。
