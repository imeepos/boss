# 一线演练前置核查：25efa98a 上线状态确认（2026-08-29）

走查人：何佳（客服运营代表）。任务：新小区交付当天场景 CDP 真实走查。
**结论：前端 25efa98a 未上线 102，走查暂停，等通知；不得自行触发部署。**

## 一、前端未上线证据链

1. 102 前端入口 `http://192.168.0.102:5180/` → `assets/index-Cb7RTl8M.js`（构建产物时间戳 2026-08-29 08:42）。
2. 提取主 bundle 内全部懒加载 chunk 清单 96 个，并发全量下载到 /tmp/hj-chunks。
3. 用 25efa98a 提交内新增特征串全量 grep，**全部 0 命中**：
   - 「该层级已存在同名地址,请点击面包屑返回对应层级选择已有节点」（chainDuplicate zh）
   - 「已回填客户档案地址,下一单无需再建」（chainBackfilled）
   - 「按区域规则推导」（chainOwner）
   - 「请先选择客户」（chainNeedCustomer）
4. 40900（UNIQUE path 撞库→软提示引导）仅出现在分类页 chunk（categories-D8SavTz8.js、index-CdhN3O-k.js，site-categories 占用错误），地址链相关逻辑不存在。
5. needsReview 唯一命中的 index-pruAehR1.js 为地址管理页列表过滤参数（`/addresses?needsReview=1`），非 AddressChainDrawer。

## 二、后端已就绪（可随前端上线立即走查）

- `POST /api/admin/v1/orders/address` 空 body → HTTP 200 code=42200「customerId must be a positive integer」：路由存在、入参校验生效，与提交信息「customerId=0 会以 42200 失败」的防线一致。
- `GET /api/admin/v1/addresses/search?q=QA-HJtest` → `{"code":0,"data":[]}` 正常。

## 三、测试数据现状

- QA-HJ 前缀地址：零残留。
- QA-HJ 前缀客户：零残留。
- 可用既有测试客户：id 213「采购经理·王」、214「李工程师」、215「王经理」、216「陈小姐」、217「张博士」。

## 四、走查计划（等上线通知后执行，勿改）

场景：QA-HJtest苑 交付当天，树上无小区无楼栋。
路径：开单抽屉 → 选测试客户 → 搜地址无结果 → 新增地址 → 逐级先搜后建五级 → 创建 → 自动选中 → 提交成功。
核对五条：①全程表单零丢失（最低要求）②上级命中即选不重填 ③建楼栋时同小区已有楼栋名参照（无则记体验缺口）④「按区域规则推导」警示条+兜底黄标可懂不慌 ⑤「待治理」黄标语义可理解（不影响开单、后台会治理）。
记录维度：每步顺畅/卡顿/困惑 + 是否被引导跳页/找管理员/重填。
产出：走查记录 + 一线结论 + 体验改进清单（≤3 条，按痛点排序）。发现问题不改代码。
