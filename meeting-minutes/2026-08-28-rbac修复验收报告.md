# RBAC 五项修复 102 真实环境验收报告（2026-08-28）

- 验收对象：fix1~fix5（客户单资源范围 / 订单详情族范围 / 停用 token 吊销 / partner region-scope / install-logs 收权）
- 验收方式：**102 部署环境真实接口探测**（非单元/集成测试），CI(deploy-102) 推送 main 后自动部署，boss-server 21:04 重启承载新二进制；迁移 000168 已入库（schema_migrations 首行）。
- 脚本：`scripts/rbac-102-acceptance.sh`（造号→探测→清理，幂等可重放）
- 结果：**PASS=15 FAIL=0**；测试账号 4+1 个与探针客户全部清理，DB 复核 0 残留；存量订单 ORD-20260828-000575 状态未被触碰。

## 逐项证据（原始 FAIL → 修复后实测）

| 原始缺陷 | 验收探针 | 修复前 | 修复后实测 |
|---|---|---|---|
| D-08 客户单资源越权读 | ops(rs=root.luzon) 读 visayas 客户(id=345) vs 不存在(999999) | 200 完整档案 | 两响应**逐字节一致**：`{"code":40400,"data":null,"msg":"资源不存在"}`；范围内客户(luzon id=213)仍 200 可达 |
| GetByNo 订单详情族 | ops 读他企订单 ORD-20260828-000575(le=6) vs NOPE-404；越权 cancel | 200 数据回显 | 读与 cancel 均返回同族 40400 响应；**cancel 后 DB 复核订单状态仍 CANCELLED 未被触碰** |
| 停用号旧 token 全权可用 | 建号→登录→停用→旧 token 打 /auth/me | 200 | `{"code":401,"msg":"account disabled"}`；重新登录 `40100` 拒 |
| partner region-scope 完全不可用 | pt_admin(le=7)：设 NULL 公共区 root.visayas / 清空 / 设外资区 root(企业6) | 一律 code=50000 | 公共区设置 `code=0`；清空 `code=0`；外资区 `40300 无权限`（业务码归位，原 500） |
| /install-logs 无门禁 | technician / ops / admin 各调 GET /install-logs?ticketId=221 | tech 可读全部回单 | tech `403 no permission: menu:install-board`；ops `200`（看板保持可用）；admin `200` |

## 正控（防误伤）

- ops 范围内客户可达（P1 PASS）；ops/admin 的施工看板接口保持可用；订单数据零改动。

## 过程记录

- 修复经 5 个独立 worktree 分支（fix/rbac-01~05）逐项实现、`go test ./internal/...` 62 包全绿、逐项 ff-merge 进 main，期间并行会话推进 main 两次，均按 worktree 协议 rebase 后重试合并，零冲突零丢失。
- 验收脚本两轮脚本缺陷（tok 密码硬编码 / req 参数位错）当场定位修正；P6 预期曾写反（root 系企业 6 所有、非公共区），经 DB 取证后按真实语义修正断言。
