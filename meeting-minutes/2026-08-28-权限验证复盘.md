# 复盘：角色账号数据/菜单权限验证（2026-08-28）

> 会议主持人复盘。关联：`2026-08-28-角色权限测试.md`（主纪要）、`2026-08-28-权限测试执行.md`（实测）、`2026-08-28-menu-perm-diff-baseline.md`（基线）。

## 一、已完成

| # | 事项 | 产出 |
|---|---|---|
| 1 | 五成员评审会（陈静/老周/小林/安琪/何平），全部发言收录 | 主纪要第三节 |
| 2 | 会前事实核查：102 库账号实查、/auth/me 与 /menu-perms 只读探测、三处代码核实（region_scope 空=全集团、GetByNo 裸查、DataAuthz 零接线） | 主纪要第二节，battle 裁决与疑点复核全靠它 |
| 3 | 9 角色 FE/BE 权限 diff 对齐基线（小林） | menu.def 85 页 × ROLE_GROUPS × menu-perms 三方对账 |
| 4 | Battle 何平 vs 老周：跨企业拦截判定口径，1.5 轮收敛 | 判定 oracle：功能层 403 统一提示；数据层可区分性检验；**200 回显一票否决**；写路径信 DB 复核 |
| 5 | 产品裁定 ×2（何平）：落地页路由驱动为 P0；menu:product 拆读写码收窄 | 均含可执行判据与验收落点 |
| 6 | 102 实测（陈静）：9 个 e2e-rbac-* 造号、53 用例、220×2 GET 全扫描、当日清理双复核残留 0 | M 组 21 PASS；D/V 抓出缺陷 |
| 7 | 缺陷确认 ×3+1：customer 单资源越权读 200、partner region-scope NULL 不可用、停用不吊销 JWT、install-logs 暴露面 | 代码+黑盒双证实 |
| 8 | 疑点全闭环 ×5：technician 授权不可达（实锤）、sysadmin 零漂移、region_scope 语义、e2e 临时号堆积、partner-audit 非错授 | 主纪要第五节 |
| 9 | 纪要落盘 + git 存档 + skill 经验沉淀 | 提交 c4f0f388、e28e36c5 |

## 二、待办未做（12 条行动建议，均未落地为代码/PR）

**P0 安全缺陷（后端，老周）**
1. `customerGetHandler` 补 DataScope（D-08 FAIL）
2. `GetByNo` 及 cancel 等按单号寻址的写路径补 le/region 过滤（或接通 DataAuthz）
3. 停用账号吊销存量 token（Authn/HasPermission/GetDataScope 至少一处补 status 校验）

**P1 功能修复与验收基线**
4. partner region-scope `COALESCE(legal_entity_id,0)` + 错误码映射（功能完全不可用）
5. 前端落地页由 /menu-perms 驱动 + 删未知角色兜底（九角色冒烟 0 个"可见即 403"）
6. `/install-logs` 补 permCode 与数据范围
7. 存量 15+ 个 e2e-/loadtest sysadmin 号清理 + 每日巡检门禁
8. M/D/V 用例沉淀为 CI 可跑的回归套件

**P2 防复发与完善**
9. menu:product 拆 product:read/write 迁移
10. 菜单权限三方对账 CI 门禁
11. data-layers.md 补越权响应样例契约
12. 前端补 menu:ai/partner-audit 缺页、RoleCode 枚举与后端 9 角色对齐、dispatch_li 脏数据处置（待产品定性）

## 三、过程中暴露的缺陷与待完善问题

**被测系统（本轮成果）**：见上 P0-P2，均为真实缺陷，非流程问题。

**会议/协作流程本身**
1. 成员 subagent 首轮 4/5 failed（与当日早间 UI 审查会同根，累犯第 2 次）——已沉淀"纯文字重催"修法入 lessons + recidivism。
2. 并行提交扫脏：某成员收尾 `add -A` 把 5 个不相关文件混进一个主题 commit（fc0bd47c），破坏可独立 revert 性——已登记新坑；纪要最终版靠后续提交 c4f0f388 收口，无内容损失。
3. battle 消息时序交错（老周收到第 2 轮后才回第 1 轮），记录完整但归档时需人工对齐轮次。

**测试覆盖盲区（诚实清单）**
4. **partner 订单行过滤未用真实数据验证**：库内 partner 域订单=0，D-03 空集"不能单独定论"，隔离结论靠 staff 成员与写路径佐证——回归套件造数时应含 partner 域订单。
5. **custom_* 自定义角色未实测**：线上 9 角色无 custom_* 行，动态菜单推导（menu:<key> 逐项过滤）属纸面验证；待首个自定义角色创建后补测。
6. **UI 层断言未做**：何平要求的"点菜单+直访 URL 双路径"只完成接口侧，浏览器级（CDP 菜单渲染断言、逐页直访 403）未执行。
7. **带 total 的列表接口未系统排查**：老周提出"列表 total 必须与明细同 WHERE"，order.List 不回 total 未涉，其他列表未扫。
8. 写探测为抽样（3 组角色 × 核心写接口），非全量写接口矩阵——已由 220 GET 全扫描 + 显式授权归因部分兜底。

## 四、下一步建议（按优先级）

1. **本周**：老周修 P0 三项 + partner region-scope（1 项 P1），修复后按 battle oracle 跑 D-08/GetByNo 回归；这是本轮测试的价值兑现点，不修则测试白做。
2. **下个迭代**：小林落地页路由改造（P0 已裁定）+ install-logs 收权 + 存量号清理入巡检。
3. **随回归套件**：用例沉淀入 e2e/，补 partner 订单造数、custom_* 角色用例、CDP 双路径断言、total 一致性排查——把本轮 4 个盲区变成用例。
4. **制度化**：三方对账 CI 门禁 + data-layers.md 契约样例，防两套真相源再漂移。

## 五、一句话总结

本轮用一场五成员评审会（battle 1.5 轮收敛判定口径）加 102 环境实测（9 造号、53 用例、220×2 全扫描、当日清零），证实 9 类角色权限模型主体正确（菜单矩阵零差集、越权写全拦、暴露面干净），同时抓出 3 个真缺陷（客户单资源越权读、partner region-scope 不可用、停用不吊销 JWT）并沉淀 12 条行动项与 3 份纪要；无过度设计——battle 即收敛不空转、测试全复用现有接口与 curl 未造新框架、造号当日清，唯一超出"抽样必要范围"的 220×2 全扫描直接换来了"0 异常差集"的干净结论，属于花得值的深入。
