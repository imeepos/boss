# 小区名历史联想·前缀过滤 手工确认报告（观察②遗留项）

- 确认人：郑稳（测试/QA）
- 日期：2026-08-29（设备时钟 CST 凌晨）
- 设备：MI 9 SE (ee9999eb) 在线；com.ymm.boss.user（lastUpdateTime=08-29 04:20:26，含抛光轮提交）
- 方法：uiautomator 坐标 tap（与自动化同款手法）+ input text/keyevent 组合；因联想浮层在软键盘打开时不在 a11y dump 中，部分断言采用「收键盘后 tap 字段重展开浮层」的可见窗口完成
- 实现对照：`AddressFormFields.kt` CommunityField——`candidates = value.isBlank() ? 全量 : startsWith(value, ignoreCase=true)`；`menuShown = expanded && (空 || candidates 非空)`；无命中输入即撤销展开（收起后不自动重弹）

---

## 确认① 空输入点字段 → 全量历史展开，表单不误关 — **PASS / 非缺陷**
- 新增地址表单内 tap 小区字段（placeholder 实测 bounds [96,945][340,1017]，中心 (418,981)，与自动化所用 y=945-1017 区域一致）。
- 结果（多次复现 ≥4 次）：**列表正常展开，全量 6 项** = `HOME / Green Park / Commonwealth / 8号楼 / B3验证小区 / Barangay 6`，恰为库内 6 条地址的 community 去重全集（已删的 Barangay 4 不在，证明列表来自实时数据而非硬编码）。
- **表单 0 次误关**。
- 关于主持人遇到的「tap 触发 ModalBottomSheet 整体关闭」：本轮同手法多次执行未复现（0/N）。本轮观测到的唯一关闭路径是 `input text` 纯字母在软键盘弹出瞬间投递时发生（搜狗 IME 吞字 + 事件时序伪影），换用「先收键盘再输入」或键码注入后同路径不再复现。**判定：自动化时序伪影，非真实缺陷，不需要 M3 升级专项。** 附自动化注意事项：tap 字段后需等待键盘弹出动画完成（≥2.5s）再取坐标/做后续动作，否则事件会按旧布局命中错误字段（本轮实测 tap 落到楼栋/单元字段即此原因）。

## 确认② 非空输入 → ignoreCase 前缀过滤 — **PASS**
- 输入 `8` → 浮层菜单可见项只剩 **`8号楼`**（唯一命中，其余 5 项全部被滤掉；tap 展开后 dump 实证 `menu=['8号楼']`）。
- 附加 ignoreCase 实证：输入小写 `bar` → 菜单可见项只剩 **`Barangay 6`**（大写 B 开头的历史项被 ignoreCase 命中；HOME/Green Park/Commonwealth/8号楼 均被正确排除）。
- 上述两项直接满足「非空=ignoreCase 前缀过滤」。

## 确认③ 无匹配输入 → 列表收起 — **PASS**
- 输入无命中内容（实测用 `8bar`，亦等价于 zzz 类乱串）→ 菜单收起（展开尝试下无可 dump 菜单项）✓，表单与其他字段不受影响。
- 与实现一致的细节：无命中会撤销展开标记，**收起后清空内容/退格回命中区不会自动重弹**（需再点字段；实现注释明示此为防"浮层压在打字手指下"的设计）——建议在产品文档中注明该交互。

## 重要观察项（不影响三项结论，建议产品/开发知悉）
**软键盘弹出会收起联想浮层**：点字段 → 浮层展开 → 键盘弹出瞬间浮层即收起；打字过程中浮层不可见，需收起键盘或再点字段才能看到过滤结果。这与"打字时浮层实时变短"的交互预期存在落差（ExposedDropdownMenu 在 IME 窗口获取焦点时被 dismiss 的固有行为）。真实用户主路径（点字段看历史 → 直接点选；或打字后收键盘看结果）均可完成，但"边打字边看候选"体验不成立。建议评估：M3 版本升级验证 / `MenuProperties(focusable=false)` / 改用自定义下拉层。

## 清理
- 表单内验证数据（`bar`）未保存，直接退出；DB 复核 `user_addresses` 仅剩既往 6 条（id1~7，id6 原样）✓
- App 已退后台（HOME，Launcher 聚焦）✓；网络在线（`Active default network: 104`）✓

## 结论

**三项确认全部 PASS，判定为正常实现（非缺陷）**，与苏晚裁定 A 的预期行为一致（空=全量、非空=ignoreCase 前缀、无匹配=收起）；「tap 误关表单」在本轮 uiautomator 手法下未复现，判定为自动化时序伪影（附规避要点），无需 M3 1.4.0 专项评估。唯一建议跟进项：软键盘弹出导致联想浮层收起的交互落差，转产品/开发评估。
