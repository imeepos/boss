# 地址选择器抛光轮 真机验证报告

- 测试人：郑稳（测试/QA，Android 真机自动化）
- 日期：2026-08-29（设备时钟 CST；对应服务端 08-28 20:1x~20:3xZ）
- 设备：MI 9 SE (ee9999eb)，com.ymm.boss.user versionCode=2，**lastUpdateTime=08-29 04:20:26**（抛光轮新包装机确认；DEX 检索「区域编码为旧格式」×1、「当前：」×1 均在包）
- 后端：102:28080 正常
- 范围：P1/P2/P3 三项新行为 + 回归抽查（A2 级联、D4 手机号）

---

## P1 确定性 miss 不再显示重试 — **PASS**
- id=6（郑稳B3，`ncr.manila.barangay_6`）编辑页：「所在区域」行显示裸段 **`barangay_6`** + 灰字 **`区域编码为旧格式`**；dump 全树 grep「重试」= **0 处**（前轮的「点此重试」入口已按设计移除）✓
- 新路径对照（id=3 Commonwealth）：区域行正常回显 `国家首都区 · Quezon City · Commonwealth` + `已绑定层级路径`，grep「旧格式/点此重试」= 0 ✓ 原有回显不受影响。
- 纪律遵守：id=6 只读未改未删（测后库校验 `detail='B3验证小区  '`、path 原样）✓

## P2 编辑态电话掩码提示 — **PASS**
- 前置说明：上轮存 09171234567 的 id11 已按纪律删除，本轮重造（见回归 D4，id=12）。
- 编辑 id12：电话输入框下方出现 **`当前：091****4567（留空保存则保持原值）`**——掩码与库值吻合，且附带"留空=保持"语义提示（超出验收要求的完整度）✓
- 新建地址页：维持原文案 **`留空则使用账户手机号`**，无掩码提示（新建无"当前值"，正确）✓

## P3 我的页地址徽标联动 — **PASS**
| 步骤 | 库内条数 | 徽标显示 |
|---|---|---|
| 基线 | 6 | **6个地址** ✓（修复前恒为 0） |
| 新增 id12 后 | 7 | **7个地址**（+1）✓ |
| 删除 id12 后 | 6 | **6个地址**（-1）✓ |

安栋实现方式核实（`ProfilePage.kt`）：`addrCount = data.optJSONArray("addresses").length()`，数据源为 profile 接口的 addresses 数组（权威、单一）；`LaunchedEffect(nav.refreshTick)` 保证每次进入我的页重新拉取，增删后回页即刷新。徽标语义=地址数量，实现合理 ✓。未发现"语义本不是地址数量"的特殊判定或注释。

## 回归抽查 — **PASS**
- **A2 级联主流程**：新增地址 → cascade NCR→City of Manila→Barangay 4 → 区域行回填 `国家首都区 · City of Manila · Barangay 4` + `已绑定层级路径` + 小区名自动回填 `Barangay 4` → 保存 → 列表出卡（id=12，`address_path=ph13..1004`）✓
- **D4 手机号**：编辑 id12 数字键码注入 `09171234567` → 保存 `PUT /api/user/v1/addresses/12 → 200` → 库 `phone=09171234567` ✓ 抛光未误伤。

## 过程备注（非缺陷）
- versionCode=4 更新弹窗冷启动照旧出现，按预案点「忽略」，未影响验证。
- 一次删除确认弹窗被我的导航误操作取消，重试后删除成功（测试操作因素，非 App 问题）。
- 一次误触拉起 MIUI 系统搜索「用户须知」，关闭后无影响。

## 清理与留痕
- 测试数据：id12 已经 UI 删除（确认弹窗→删除），DB 复核仅剩既往 6 条（id1~7），id6 原样（`detail='B3验证小区  '`，path `ncr.manila.barangay_6` 未动）✓
- 网络：在线（`Active default network: 104`）✓；App 已退后台（Launcher 聚焦）✓

## 总结论

**合并。** P1/P2/P3 三项抛光全部验证通过且实现方式合理，回归抽查（A2 级联主流程、D4 手机号保存）无误伤，id=6 治理资产零改动，测试数据已清理。
