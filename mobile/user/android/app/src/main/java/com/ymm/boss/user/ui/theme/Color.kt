package com.ymm.boss.user.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * 设计稿 F 节色板,浅色/深色成对。
 * 页面内禁止硬编码 Color(0xFF...),新增色值一律先落这里并配深色值。
 */
val BrandBlue700 = Color(0xFF273F70) // 主色(浅色)
val BrandBlue700Dark = Color(0xFF9CB8E8) // 主色(深色,弱化保证暗底可读)
val SurfaceDark = Color(0xFF1E1E1E) // 表面(深色)
val SuccessGreen = Color(0xFF4CAF50) // 成功(浅深同值)
val AuxGray = Color(0xFF9E9E9E) // 辅助(浅深同值)
