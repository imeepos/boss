{
  "name": "boss-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "BOSS 管理端桌面客户端:Tauri 2 壳,内嵌 web/admin 构建产物,/api 由壳内反代转发 Go 后端",
  "scripts": {
    "tauri": "tauri",
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
