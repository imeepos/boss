import { useEffect, useState } from 'react'
import { apiFetch } from '../../../api/client'
import { useT } from '../../../i18n'
import { PageHead, pagerTexts } from '../../org/shared'
import { Pagination } from '../../../components/Pagination'
import { TableStateRow } from '../../../components/business'
import { useConfirm } from '../../../components/ConfirmDialog'
import { Dropdown } from '../../../components/Dropdown'

type Article = { id: number; code: string; title: string; content: string; status: string; version: number; ownerId: number; updatedAt: string }

export default function KnowledgePage() {
  const t = useT(); const k = t.pages.knowledgePage; const confirm = useConfirm()
  const [rows, setRows] = useState<Article[]>([]); const [busy, setBusy] = useState(false); const [error, setError] = useState(''); const [page, setPage] = useState(1); const [pageSize, setPageSize] = useState(10)
  const [editing, setEditing] = useState<Article | null | undefined>(undefined); const [form, setForm] = useState({ code: '', title: '', content: '', status: 'DRAFT' })
  const load = () => { setBusy(true); setError(''); apiFetch<{ items: Article[] }>('/knowledge-articles').then((d) => setRows(d?.items ?? [])).catch((e) => setError(e instanceof Error ? e.message : k.loadFail)).finally(() => setBusy(false)) }
  useEffect(load, [])
  const open = (a?: Article) => { setEditing(a ?? null); setForm(a ? { code: a.code, title: a.title, content: a.content, status: a.status } : { code: '', title: '', content: '', status: 'DRAFT' }) }
  const save = async () => { setBusy(true); try { await apiFetch(editing ? `/knowledge-articles/${editing.id}` : '/knowledge-articles', { method: editing ? 'PUT' : 'POST', body: form }); setEditing(null); load() } catch (e) { setError(e instanceof Error ? e.message : k.saveFail); setBusy(false) } }
  const remove = async (a: Article) => { if (!(await confirm(k.deleteConfirm, { danger: true }))) return; setBusy(true); try { await apiFetch(`/knowledge-articles/${a.id}`, { method: 'DELETE' }); load() } catch (e) { setError(e instanceof Error ? e.message : k.actionFail); setBusy(false) } }
  const slice = rows.slice((page - 1) * pageSize, page * pageSize)
  return <div><PageHead title={k.title} desc={k.desc} /><div className="mb-4 flex justify-end"><button className="rounded-sm border border-[var(--shell-input-border)] px-3 py-1.5 text-xs" onClick={() => open()}>{k.create}</button></div><div className="rounded-md border border-[var(--shell-card-border)] bg-[var(--shell-card-bg)] p-4">{error && <div className="mb-3 text-sm text-[var(--color-danger)]">{error}</div>}<div className="overflow-x-auto"><table className="w-full border-collapse text-[13px] text-[var(--shell-content-text)]"><thead><tr>{k.columns.map((x) => <th key={x} className="border-b border-[var(--shell-side-border)] px-3 py-2 text-left text-xs">{x}</th>)}</tr></thead><tbody>{slice.map((a) => <tr key={a.id}><td className="border-b border-[var(--shell-side-border)] px-3 py-2">{a.code}</td><td className="border-b border-[var(--shell-side-border)] px-3 py-2">{a.title}</td><td className="max-w-md truncate border-b border-[var(--shell-side-border)] px-3 py-2">{a.content}</td><td className="border-b border-[var(--shell-side-border)] px-3 py-2">{a.status}</td><td className="border-b border-[var(--shell-side-border)] px-3 py-2">v{a.version}</td><td className="border-b border-[var(--shell-side-border)] px-3 py-2"><button onClick={() => open(a)}>{k.edit}</button><button className="ml-3 text-[var(--color-danger)]" onClick={() => remove(a)}>{k.delete}</button></td></tr>)}{!slice.length && <TableStateRow colSpan={6} loading={busy} text={k.empty} />}</tbody></table></div><div className="flex justify-end pt-3"><Pagination total={rows.length} page={page} pageSize={pageSize} onPage={setPage} onSize={setPageSize} {...pagerTexts(k)} /></div></div>{editing !== undefined ? <div className="mt-4 rounded-md border border-[var(--shell-card-border)] bg-[var(--shell-card-bg)] p-4"><div className="grid gap-3 md:grid-cols-2"><label className="text-xs">{k.fCode}<input className="mt-1 block w-full rounded-sm border border-[var(--shell-input-border)] bg-[var(--shell-input-bg)] px-3 py-2" value={form.code} onChange={(e) => setForm({ ...form, code: e.target.value })} /></label><label className="text-xs">{k.fTitle}<input className="mt-1 block w-full rounded-sm border border-[var(--shell-input-border)] bg-[var(--shell-input-bg)] px-3 py-2" value={form.title} onChange={(e) => setForm({ ...form, title: e.target.value })} /></label><label className="text-xs md:col-span-2">{k.fContent}<textarea className="mt-1 block min-h-32 w-full rounded-sm border border-[var(--shell-input-border)] bg-[var(--shell-input-bg)] px-3 py-2" value={form.content} onChange={(e) => setForm({ ...form, content: e.target.value })} /></label><label className="text-xs">{k.fStatus}<div className="mt-1"><Dropdown value={form.status} options={k.statusOptions.map((x) => ({ value: x, label: x }))} onChange={(value) => setForm({ ...form, status: value })} ariaLabel={k.fStatus} /></div></label></div><button className="mt-4 rounded-sm border border-[var(--shell-input-border)] px-4 py-2 text-xs" disabled={busy} onClick={save}>{k.save}</button></div> : null}</div>
}
