# T14-1 验收:GIS 点位点击反查弹窗(intel/gis)

> 日期 2026-09-07(账本键名 2026-09-08)| 分支 feat/t14-gis-order-rider | 执行:专属会话
> 机械门禁:`bash scripts/accept/pp2-gate.sh web/admin/src/pages/intel` → **PP2 GATE PASS / GATE_RC=0**(/tmp/t14-gate2.log,worktree 内实跑)

## 交付内容

- 提交 1ce1ef52 i18n(gisPage): rev* 键族三语 append-only(types.ts + zh-CN/en-US/ms-MY,128 行)
- 提交 ff738717 feat(gis): OdnReverseDrawer.tsx(152 行)+ odnReverse.ts(119 行)+ index.tsx 点位分流(level 9-11 → 反查抽屉;6-8 → 既有资源详情;互不干扰)
- 提交 0bbca2d9 fix(gis): resolve 契约 distanceM omitempty → 自指点位距离按 0m 呈现
- 零后端改动:全部编排既有只读接口(/odn/facilities/:code、/odn/sites、/odn/devices、/odn/devices/:id/ports、/odn/bindings?portId、/odn/grids、/odn/coverage/resolve)

## 102 环境实测(全部真实接口,无 mock;admin/admin123 @ http://192.168.0.102:28080)

### 验收造数(验收后保留,均为 ODN 演示资源命名 T14 demo *)

| 动作 | 接口 | 结果 |
|---|---|---|
| 地址 292 补坐标 | PUT /addresses/292/geom {14.5995,120.9842} | code=0 |
| 设施 P91012(电杆,91 网格) | POST /odn/facilities | code=0;odn-points 层出现 (id=1,level=9) |
| 设施 MH91011(人井,视口内远离点) | POST /odn/facilities | code=0 |
| 局点 MNL001/MNL002 | POST /odn/sites?prvCode=PHL001&cityPrefix=MNL | code=0(nodeCode 回显) |
| 设备 OLT001(挂 MNL001)/ SNW001(市域) | POST /odn/devices | code=0;SNW001 id=192,OLT001 id=191 |

### 接口实测记录

- GET /gis/odn-points?entity=facility|site|device → 三层各自返回带坐标点位(facility: P91012/MH91011;site: MNL001/MNL002;device: OLT001/SNW001)
- GET /odn/coverage/resolve?lat=14.5995&lng=120.9842 → `{status:SERVED, facilityCode:P91012, distanceM:873.80}`
- GET /odn/coverage/resolve?lat=14.5&lng=120.75 → `{status:UNSERVED, facilityCode:P91012, distanceM:28346.72}`
- GET /gis/resources/237/detail → `{code:50000,msg:内部错误}`(存量后端行为,见「限制」)

## 走查记录(cdp-capture CDP 可信点击,2026-09-07 实测)

> 工具:.agents/skills/self-evolving/scripts 改造版 /tmp/t14-cdp.mjs(免登录注入 + Input.dispatchMouseEvent 可信鼠标事件;OL 地图不吃合成 MouseEvent,已实测二连失败后改 CDP Input)。每步断言为页面内 DOM 实测返回值,非人工目测。

### 需求 1:点位资源人类可读信息

- 点击设施点位 P91012(120.99,14.605)→ 抽屉显示:编码 P91012 / 名称 T14 demo pole / 类型 电杆 P / 状态 在用 / 生命周期 在网 / 坐标 14.60500,120.99000。断言 `code/name/kind/status/life=true`。
- 点击设备点位 OLT001(120.9895,14.6045)→ 类型 OLT 设备 / 状态 在用 / 生命周期 在网。断言全 true。
- 点击设备点位 SNW001 → 类型 熔纤盘 SNW。断言 true。
- 枚举全部中文映射(设施 P/MH/TW/CLS/TBX,设备 SNW/OLT/...十类,生命周期四态,状态 IN_USE/ACTIVE/RETIRED,端口 IDLE/RESERVED/IN_SERVICE),未知值回退原码不臆造。
- 截图:docs/acceptance/shots/t14-1-light-02-facility-drawer.png、t14-1-light-04-device-drawer.png、t14-1-dark-01-facility-drawer.png(双主题)、t14-1-en-facility-drawer.png(en-US)。

### 需求 2:绑定关系反查

- 设备点位 OLT001:抽屉「业务绑定」区经 GET /odn/devices/191/ports → 0 个非 IDLE 端口 → 逐口 /odn/bindings?portId 未触发 → 显示空态文案「该点位暂无端口绑定记录」。断言 bindEmpty=true。
- 展示口径:绑定行 = 端口 P{No} + 状态中文 + 订单 #id + 绑定时间;上级设备/所属局点均显示名称(OLT001 抽屉显示所属局点「T14 demo site」),设施显示所属网格名(acc-grid-91),无裸编号。
- 截图:t14-1-light-04-device-drawer.png。

### 需求 3:可装性三态徽标

| 点位 | 坐标 | resolve 实测 | 徽标 | 距离 |
|---|---|---|---|---|
| P91012 设施 | 14.605,120.99 | SERVED | 可装(绿) | 0m(自指,omitempty 回退) |
| SNW001 设备 | 14.5,120.75 | UNSERVED,distanceM=28346.72 | 不可装(红) | 28347m |
| 待覆盖(PENDING) | — | 坐标判定路径不产生该值(ResolveLatLng 只返回 SERVED/UNSERVED) | 徽标组件已实现 PENDING→蓝(info),**未验证**(见限制) | — |
- 截图:t14-1-light-02-facility-drawer.png(绿)、t14-1-light-03-device-unserved.png(红+28347m)。

### 需求 4:反馈链路四态

| 态 | 实测 | 证据 |
|---|---|---|
| 点击响应 | trusted click 后抽屉立即开启,骨架加载态(Spinner+加载中)先行 | 各 run click-eval 至断言间隔内抽屉已出现 |
| 等待 | loadOdnReverse Promise 期间 role=status 骨架 | OdnReverseDrawer.tsx Phase=loading |
| 成功 | 数据渲染分区展示(资源信息/ODN 图层/业务绑定/可装性) | t14-1-light-02-facility-drawer.png |
| 失败可复制 | CDP 注入 fetch 拒绝(/odn/facilities/ → reject 'T14-FETCH-FAIL ECONNRESET 模拟中断')→ 红色横幅「反查加载失败: T14-FETCH-FAIL ECONNRESET 模拟中断」+ 复制按钮(CopyButton,clipboard API/降级 execCommand)+ 重试按钮;恢复网络后点击重试 → 抽屉恢复完整数据(断言 recovered=true,dist=true) | t14-1-light-05-fail-state.png、t14-1-light-06-retry-recovered.png |

### 与既有 drill 点位共存

- 切层级下拉至「6. 弱电井(OLT)」→ 地图出现既有 drill 点位「演示OLT」(id=237)→ 可信点击 → 既有「资源实时详情」抽屉开启(DRILL_OK,DOM 实测)。该详情接口对本演示资源返回 50000(存量行为,非本次改动),抽屉按既有失败路径显示「详情加载失败」。
- 同会话先开 drill 抽屉→关闭→开 ODN 层→点击 ODN 点位→反查抽屉开启,两类点击行为互不干扰。
- 截图:docs/acceptance/shots/t14-1-light-01-drill-coexist.png。

## 限制与未验证项(如实声明)

1. **含数据的绑定行未端到端验证**:102 环境无任何 odn_port 行(应用层无端口创建接口,端口行仅迁移建表),绑定反查实测只能覆盖空态;绑定行渲染路径(端口↔订单行)已实现并通过门禁/类型检查,标注**未验证**。绑定对端订单以「订单 #id」呈现——orderId→orderNo 无既有只读接口,受「不改后端」约束无法解析为订单号,属已知展示限制。
2. **待覆盖(蓝)徽标未验证**:坐标就近判定接口只产 SERVED/UNSERVED;PENDING 仅存在于 address_coverage 登记记录,坐标路径不可达。徽标映射已实现。
3. i18n ms-MY 键已同步并过门禁,浏览器级 ms-MY 截图未单独采集(与 en-US 同机制,低风险)。

## 证据文件

- 门禁日志:/tmp/t14-gate2.log(PP2 GATE PASS,GATE_RC=0)
- 网络采集:/tmp/t14-1-runA..G.json(console/网络/失败请求体)
- 截图 8 张:docs/acceptance/shots/t14-1-{light-01-drill-coexist,light-02-facility-drawer,light-03-device-unserved,light-04-device-drawer,light-05-fail-state,light-06-retry-recovered,dark-01-facility-drawer,en-facility-drawer}.png