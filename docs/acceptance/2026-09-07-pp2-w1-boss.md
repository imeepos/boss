# PP2-W1 boss 域体验打磨 · Phase B 验收(2026-09-07)

> 分支 feat/pp2-w1-boss(基于 main@bef4e5ad,W0 选择器基座合并后)· 专属会话 PP2-W1
> 依据:docs/plan/pp2-audit-boss.md(Phase A 审计)+ 负责人 Phase B 指令三项裁定
> 环境后端 http://192.168.0.102:28080;复验载体 = 本地 vite dev(5199,proxy→102)逐页 cdp 实截+DOM 断言,截图留 /tmp/pp2-w1/(不入库)

## 一、机械门禁(worktree 实测)

| 步骤 | 命令(TZ=Asia/Shanghai) | 结果 |
|---|---|---|
| typecheck | npx tsc --noEmit | 0 错误 |
| test | npx vitest run | 81 文件 / 523 用例全绿 |
| build | npx vite build | ✓ built,BUILD_RC=0 |
| 卫生红线 | git grep alert[(] / <select(boss 域) | 双零命中 |
| 单文件行数 | boss 全部 tsx 最大 | 258(<300) |
| 值契约(裁定②) | 静态 grep value={xLabel(...)} boss 域 | 0 命中;运行时 16 页采集 '[Dropdown] value 不在 options 值域' 告警 0 条 |
| ams 门禁红 | web-admin-check 整体含 ams 采用率 41% FAIL | 与 boss 无关,W2 波次范围,Phase A 已登记移交(见 docs/plan/pp2-audit-boss.md §六) |
| 终版 16 页巡检 | cdp 实截+日志扫描(本地 dev→102) | 16/16 OK:console 错误 0、Dropdown 值契约告警 0、网络失败 0 |

## 二、逐页处置结论(复验 = 本地 dev 实截 + DOM 断言 + console/网络零报错)

| 页面 | 修复内容 | 复验断言 |
|---|---|---|
| order | 推进/收费/取消/代客下单成功 toast;跟踪失败保因;错误条保留表格;关键词回车直查(URL 同步 kw=) | Enter 后 URL ?kw=DT-2026 + 过滤请求发出;console 0 |
| dispatch | 指派/转派 toast;错误条保留表格;页签换 TabBar 去内联裸色;#orderId/改派台账 title 留痕;fMaster/filterWorker 正名 | TabBar 三页签可见、#1677ff 零命中、6 行、console 0 |
| dismantle | 新建拆机单 toast;错误条保留表格;订单/资产/端口 label 正名;裸 ID title 留痕 | 页面渲染 1 行、console 0 |
| callback | 重试 toast;错误条保留表格;#id/#orderId title 留痕 | 页面渲染、console 0 |
| complaint | B6:OPEN 行补「受理」(POST status→PROCESSING);全行「轨迹」抽屉(GET events);办结/受理 toast;错误条保留表格;裸 ID title 留痕 | 行内 [受理][办结][轨迹] 实测;轨迹抽屉点击后 /complaints/TKT-1/events 请求发出;console 0 |
| feedback | 复核 toast;错误条保留表格;#id/#ticketId title 留痕 | 页面渲染、console 0 |
| service-metrics | 补刷新按钮 + busy 态 | 页面渲染、console 0 |
| install-board | 无写操作;登记债:无工单反查、/install-logs 未用(不改后端不新增功能) | 页面渲染、console 0 |
| worker | 队伍保存/调队/解散/师傅录入/重置密码/区域保存六动作成功 toast;成员列表错误条保留表格 | 新增师傅/新建装维队模态 labels DOM 断言;console 0 |
| worker-reg | B7:状态四页签(待审核/已通过/已驳回/全部,API status 参数)+ pageSlice 分页;通过/驳回 toast;错误条保留表格;班组/区域 label 正名;#id title 留痕 | 四页签 DOM 可见、10 行、console 0 |
| worker-ops | 健康观察/接单大厅失败保因;页签换 TabBar;#orderId title 留痕 | 页面渲染、console 0 |
| site | 分类/状态/语言三筛选值契约修正;保存/删除 toast;每页条数假控件与无关键词检索登记债 | 列表渲染、console 0 |
| site 编辑器 | 语言/分类/状态下拉值契约修正(当前值可见);保存 toast | DOM 断言 labels=「语言中文/分类动态/状态草稿」(Phase A 为「语言语言/分类分类/状态状态」);console 0 |
| site-cats | 启用下拉值契约修正;保存/删除 toast;40900 关联拒绝文案已有 | 页面渲染、console 0 |
| knowledge | 状态下拉值契约修正;保存/删除 toast | 页面渲染、console 0 |
| release | B8:编辑抽屉状态机前端设防(ALLOWED_NEXT,ROLLED_BACK 终态禁选);端筛选/上传端/编辑状态值契约修正;上传/补丁 toast;白名单仍 CSV(无列表接口,选择器登记债) | 编辑抽屉 labels=「状态草稿…」(Phase A「状态状态」);dd 告警 0;console 0 |
| message | 三页签 load/下发/发布/上下架失败保因;下发内容 textarea | 下发抽屉 labels DOM 断言;console 0 |
| license | 激活失败透传后端原因(原先全吞) | 代码路径;激活需真实激活码,登记为运行时不可逆不实操作 |

## 三、负责人裁定执行情况

1. 能力入口对齐:complaint 受理/轨迹、worker-reg 状态维度已落地;release 白名单选择器与 install-board 工单反查依赖后端联表/列表接口,按裁定①登记不新增。
2. 值契约:boss 域静态 grep 0 命中 + 终版 16 页运行时告警 0(采集日志扫描)。
3. 错误横幅:order/dispatch/dismantle/complaint/feedback/worker/worker-reg 七页全部改为「横幅 + 表格共存」。

## 四、反馈链统一覆盖

- 成功 toast 新增页:order(4 动作)/dispatch(2)/dismantle/callback/complaint(2)/feedback/worker(6 对话框动作)/worker-reg(2)/site(2)/site-cats(2)/knowledge(2)/release(2) —— sonner 覆盖从 4/32 文件增至 14 文件,其余为无写操作页。
- 失败原因保真:order 跟踪、message 三页签、worker-ops 两页签、license 激活。
- busy 态:新增动作均 disabled + 文案态(沿用既有 submitting/sending 模式)。

## 五、登记债(不阻塞验收)

- 联表可读名:dispatch/dismantle/callback/feedback/worker-ops 裸 ID 列需后端联表返回 orderNo/customerName 等(payload 现无),title 已留痕,转后端口径。
- release 白名单选择器:需白名单主体列表接口;install-board 工单反查:需工单详情入口;均按裁定①登记。
- web-admin-check 整体红 = ams 采用率 41%(W2 波次),与 W1 无关。

## 六、i18n 纪律

三语键 append-only 独立小提交共 8 个,键集一致性由 keys.test 随 523 用例锁定。

## 七、收尾状态

- 分支已 push gitea feat/pp2-w1-boss;worktree 保留待负责人合并确认(按协议四步走由负责人执行)。
- vite dev(5199)走查完毕即关停;主树与 boss-odn-p9 worktree 未触碰。
