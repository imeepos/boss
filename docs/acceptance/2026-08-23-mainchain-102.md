# 主链路验收报告(102 真实环境) 2026-08-23

> Q1 交付物:实名 → 订购 → 资源核查 → 端口预占 → 合同收费(自动段5-8) →
> 派单指派 → 扫码绑定 → 激活(自动回调+GIS) 全链路,对部署环境逐环节验收。

## 环境

- 服务: http://192.168.0.102:28080 (boss-server, admin 前缀 /api/admin/v1)
- 认证: admin API key(X-API-Key,subject account/103)
- 工具: `scripts/ops/mainchain-acceptance.sh`(每轮自举地址/分光器/端口/标签/资产,可重复)
- 客户/产品/渠道/师傅: 214(已实名 VERIFIED) / 101(PUBLISHED) / 102(HALL) / 6(王测试)

## 结果

| 指标 | 结果 |
|------|------|
| 单笔全链路(ORD-20260823-000445) | 12/12 环节 DONE,终态 stage=12 / status=DONE |
| 10 轮连续验收 | 10/10 通过,成功率 100%,单轮 8-18s |
| 阻断缺陷 | 1 个(已修复,见下) |

## 过程发现的阻断缺陷(已修复)

同地址重开单环节5 撞 `uq_quad_links_address`(23505),charge 50000:
000086 非空唯一不看 status,UNLINKED 历史行永久占坑。
修复 = migration 000110(唯一收窄到 LINKED/CONFLICT 活跃链路)+ PG 集成回归
`TestE2E_QuadLinkAddressReopen_Integration` + 复盘 `docs/postmortem/0009-*`。

## 复现方式

```bash
scripts/ops/mainchain-acceptance.sh 10
# 输出示例:
# run 1 PASS ORD-20260823-000452 (9s)
# ...
# 结果: 10/10 成功率 100.0%  总耗时 118s
```

## 遗留 finding(未阻断,待裁定)

1. 端口激活后仍停留 RESERVED,无代码路径置 USED(ports.status 枚举含 USED,
   但域层无写入点);拆机扫码解绑释放端口,正常单生命周期内不回收。
2. 扫码标签资产回填:applyTag 未选资产时扫码回填,同标签扫第二单会撞
   `uq_quad_links_asset` 活跃唯一(与四码"一资产一活跃链路"契约一致,验收脚本
   每轮新建标签规避)。
