# PP2 W2 · ams+org 域体验修复验收(Phase B,2026-09-07)

> 分支 feat/pp2-w2-ams-org(merge main bef4e5ad 取 W0 选择器基座后逐页修复);审计基线 docs/plan/pp2-audit-ams-org.md。
> 走查环境:102 部署后端 28080 + 本地 vite dev(5194,localStorage servers 注入直连 102)真实数据;cdp-admin-capture 实截+DOM 断言;截图/logs 留 /tmp/pp2-w2/b2-*(不入库)。

## 一、机械门禁

| 门禁 | 结果 |
|---|---|
| make web-admin-check(tsc --noEmit) | exit 0 全绿(TZ=Asia/Shanghai) |
| vitest src/pages(web/admin) | 50 文件 267 用例全过 |
| check-ds-adoption | **ams 24/28 = 86%(修复前 12/29 41%)**;org 23/24 = 96% 不回退;below=0 |
| console/网络 | 复验页全部 consoleErr=0、failedReq=0 |

## 二、逐页处置结论(路线图第三节对照)

| 页面 | 主要修复 | 102 复验 |
|---|---|---|
| ams/asset | 批次联表可读(RK-E2E-000031 E2E批次 替代 #1);删除失败 toast.error;型号/批次/建档表单 FormField label+TableStateRow;下拉数据源失败就地报错+重试;轨迹砍「操作人」重复列;类型筛选「全部类型」 | 实截+断言过,triggers=[全部状态,全部类型],labels 全,console 0 |
| ams/tag | **解绑接线 UnbindDialog(影响面+原因必填)**;禁用/解绑失败 toast;ErrorBanner/FormField/EmptyState;死代码 DangerOps/EventDrawer 删除 | E2E 资产 1951 绑 tag419 → 弹层影响面(目标标签/绑定资产/BOUND→UNBOUND/事件留痕)→原因必填→确认→toast「标签已解绑」→还原删资产 ✓ |
| ams/stock | 建单/关单/差异处置成功 toast;扫码资产改 SimplePicker 服务端搜索选定;实盘状态译名;处置说明就近操作条;ErrorBanner/TableStateRow | E2E 任务 #5:picker 搜 AMS-E2E→扫码 toast「扫码已记录」→处置 toast「差异已处置」→关单确认→toast「关单成功」100% 已完成 ✓ |
| ams/replace | 创建/派单/取消成功统一 sonner;ErrorBanner+表格常驻 | typecheck+提交;ResourcePicker 走 W0 兼容层零改动受益 |
| ams/purchase | 「公司 ID」数字输入(默认 1)→法人 SimplePicker 必选;供应商显式必选;明细行可删;入库确认回带订单明细;创建/提交/入库 toast;fmtAmount 兜底;占位 i18n;供应商表单 FormField | 新建抽屉断言:SimplePicker×1、删除行按钮、labels=[*供应商,*公司,备注];硬编码 number-ID 输入消除 |
| ams/inventory | materialCode 300ms 防抖;刷新 busy 禁用;批次联表可读;统计卡 2 列;ErrorBanner | typecheck+提交(防抖与 asset/tag 同口径) |
| org/company | 保存成功 toast;ErrorBanner;员工录入/重置密码/启停 toast;手机号硬编码占位去除 | 接入公司页既有 saved 词条 |
| org/staff | guard 补 okMsg toast+失败 toast;成员启停加确认(company.staff confirm);树 hover-only 改常显 | 断言 delVisible=true delCount=46(按钮常显可达) |
| org/department | 保存 toast;**补行内删除(danger confirm,40900 透传 toast.error)**;ErrorBanner | 裁定①能力入口对齐 |
| org/post | 同 department(POSTS 版) | 同上 |
| org/region | **覆盖主体先确认再改库**(assignConfirm),busy+成功/失败 toast | 实测:选未设置→弹「确认变更该区域的覆盖主体?…」→取消→无变更无 toast ✓ |
| org/menuperm | 角色/删除成功 toast+行按钮 busy;错误横幅移出表体;**权限勾选加搜索(permSearch)** | 角色表单断言:搜索权限 present,92 个权限码可过滤 |
| org/datascope | 搜索语义统一:输入防抖 300ms 走服务端 keyword | typecheck+提交 |
| org/partner | **approve 失败显式报错+reject 异常捕获**;驳回成功 toast;补检索+分页 | 断言 search=true pager=true(7 行真实数据) |
| org/apikey | **补分页+总数(实测 82 行)**;签发/吊销 toast;加载 busy | 断言 rows=10/页+上一页/下一页/条/页 ✓ |
| org/openplat | **停启应用加确认(statusConfirm)**;创建/状态/测试事件/重投/删订阅 toast;投递>20 显式截断提示;事件目录失败提示保持 | 实测:停用→弹「确认变更应用状态?」→取消→状态不变 ✓ |

## 三、负责人裁定落实

1. **能力入口对齐**:department/post 补删除(既有 DELETE API 行内入口)✓;无新页面级模块 ✓。
2. **ams 采纳率债**:41% → **86%**(24/28)✓,org 96% 不回退 ✓(check-ds-adoption 机械验证 below=0)。
3. **死代码三处**:DangerOps.tsx 删除(UnbindDialog 迁 tag/ 接线;ScrapConfirmDialog 与 asset/ScrapDialog 功能重复)、EventDrawer.tsx 删除(零引用)✓;UnbindDialog 接线标签解绑,原因经后端 reason 落 UNBIND 事件 detail(asset_handlers.go:199 已核)✓。
4. **值契约**:org 域 Dropdown 调用逐处核对,复验日志无「value 不在 options 值域」告警——不存在文案当 value 调用点(该告警为触发式)✓。
5. **错误横幅不替换整表**:两域全部列表页改 ErrorBanner+表格常驻 ✓。
6. **Toast 覆盖**:两域 25+ 写路径补 toast.success/toast.error(sonner);失败原因全文可选中复制 ✓。

## 四、服务端分页登记(裁定 4)

- 达标范本:asset、tag(服务端分页+q 防抖)。
- 本波补齐:apikey、partner(客户端检索+分页;行数量级 82/7,登记后续可迁服务端)。
- 评估登记(维持全量+前端分页,数据量小):stock、replace、purchase、inventory、org 其余列表页;超阈值再迁移,机械门禁不强制。

## 五、偏差与残留登记

- purchase 提交订单:加 toast.success,未加二次确认(状态可取消,半可逆);如需确认口径请裁定,补一条词条+ConfirmDialog 即可。
- inventory 缺仓库/库位关联列:/procurement/inventory 契约仅返回 materialCode/batchId/inStockQty,无数据源不做假列;批次列已联表可读。
- region 树形 picker:W0 无树 picker 基座,保持「下钻+确认」;覆盖主体已加确认消除危险直改。
- asset 部署地址:#addressId 兜底保留(地址名需逐行 N+1 查询,登记 /addresses 批量反查契约项);批次列已可读。
- E2E 数据:资产 1951 已删、标签 419 复原 UNBOUND;盘点任务 #5(scope=E2E-W2,DONE)按 E2E 前缀惯例留存作走查痕迹。

## 六、提交清单(自 Phase A 审计 9f26d4e5 起)

- i18n append-only 独立小提交:96adcb9f(asset filterAllType)、d4fddac1(stock×4)、51319c7c(replace createOk)、e3108927(purchase×7)、b8cc0e66(org 反馈链词条)。
- 页面修复:c3a9a5d1(asset)、90fa6e51(tag+死代码)、3f810aa5(stock)、5cc69bb2(replace)、bbc28d76(purchase)、e5f1d4fe(inventory)、48909c50(org 全域)。
