# T14-3 收尾验收:主树合并+102 部署冒烟+会话归档(2026-09-08)

## 部署通道事故与手动等效部署

- T14/W7 合并推送后 deploy-102 流水线未生效:gitea-runner 在推送窗口宕机,重启后容器内 DNS 解析 postgres 超时,拉不到任务(详见 docs/ops/2026-09-08-deploy-channel-outage.md)。
- 负责人走 CI 等效手动部署:git archive HEAD(cabf68b2)→ scp(两轮,首轮遇宿主假死负载 162 与 /tmp tmpfs 重启清空,重启后 md5 双端一致)→ 102 原生 docker build 双镜像(带授权公钥参数,GIT_SHA=cabf68b2 全串)→ 推 5000 registry 四个 tag → compose -p boss-app up -d --force-recreate。
- 健康宿主上全链 2.5 分钟完成,日志 /tmp/t14-deploy3.log。

## 部署冒烟(全部通过)

| 项 | 结果 |
|---|---|
| 五容器状态 | boss-server/boss-aaa/boss-report/boss-provisioner/boss-admin-web 全 running |
| 后端 healthz | http://192.168.0.102:28080/healthz → 200 |
| admin-web | http://192.168.0.102:5180/ → 200,新 bundle index-BC3CUOSA.js(旧为 DYYdwuCh) |
| T14 特征串 | 部署 bundle 内 反查加载失败=1 / 可装性判定失败=1 / 无法评估=1(T14-1 GIS 反查抽屉 + T14-2 下单五态徽标均已上线) |

## 会话归档

- T14 rider 专属会话 session-b6c02863 交付验收后归档(工作区会话管理 archiveSession)。

## 交接遗留

- CI 通道修复与防复发巡检:见 docs/ops/2026-09-08-deploy-channel-outage.md 待办三条(归基础设施会话)。
- T14-1 两个如实声明的限制(绑定行数据态、PENDING 徽标坐标路径不可达)不阻塞,随环境数据补齐自然可验。
