# 服务端需求 · 9 阶段渐进式拆分

（基于《需求提示词-服务端.md》拆解；每阶段独立可交付、可验收，后一阶段在前一阶段成果上扩展，不推翻已交付功能。）

## 拆分说明

1. 拆分原则：每阶段都是"一个完整可用的业务切片"，交付后即可投入实际使用；下一阶段只做增量，不改变上一阶段已验收的行为。
2. 业务闭环（装维全流程 12 环节）跨阶段渐进完成：阶段5 建立人工闭环骨架 → 阶段6 强化扫码强制 → 阶段7 实现网络侧自动化 → 阶段8 补齐 GIS 联动，最终形成完整闭环（对应关系见文末附表）。
3. 每个阶段结构统一：阶段目标 / 前置依赖 / 需求内容 / 验收标准 / 本阶段不做。
4. 方括号 [X] 为待确认参数（沿用主文档），各阶段按需填写。所有"必须"条目均为验收项。
5. 本拆分与主文档的关系：主文档是完整需求全集；本拆分是实施顺序。任一阶段的验收项都可在主文档中找到对应来源。

---

## 阶段1：基础平台与基础数据

**阶段目标**：搭建系统底座——登录、账号、角色、权限、审计、业务参数、区域地址层级，为后续所有阶段提供统一基础。

**前置依赖**：无。

**需求内容**：
1. 系统登录：以下 7 类角色可凭账号登录系统：客户、装维师傅、资产管理员、资源运维管理员、业务运营/客服人员、经营分析人员、系统管理员。
2. 角色权限：每个角色只能访问其职责范围内的功能与数据；权限可配置，权限变更即时生效，无需重新部署。
3. 操作审计：所有关键操作（数据变更、状态变更、权限变更）记录操作人、操作时间、操作内容，可查询、可追溯。
4. 业务参数：欠费停机阈值、端口预占有效期、数据核对周期、报告周期等业务规则参数集中配置、可调整。
5. 区域地址层级：建立并维护"市级 → 区级 → 街道 → 小区 → 楼栋"层级档案，作为后续资产、资源、订单、地图的统一地址基准；支持存量地址数据导入。

**验收标准**：
- 7 类角色均可登录，权限隔离生效：越权访问被拒绝并提示；
- 关键操作审计记录完整，可按人/时间/操作类型查询；
- 地址层级可逐级维护，存量地址可导入并与层级挂接。

**本阶段不做**：业务单据、资产、资源、计费、地图、认证。

---

## 阶段2：客户与产品资费（BSS 基础）

**阶段目标**：建立客户档案与产品资费体系，为下单业务提供客户与产品基础。

**前置依赖**：阶段1。

**需求内容**：
1. 客户档案：客户注册、实名信息、家庭地址（必须挂接到阶段1的地址层级，精确到楼栋）、联系方式；一人一档，可增改查。
2. 产品与资费：宽带产品、带宽套餐（如 [X]M / [X]M）、增值服务的配置、发布、下架；资费规则可配置、可调整，价格调整有生效时间。
3. 客户视图：可查询客户名下全部产品、资费、地址、服务状态。

**验收标准**：
- 客户档案完整可查，地址必须归属到具体楼栋层级；
- 产品/套餐可发布、下架，调价有生效时间，历史价格可查；
- 任一客户可查其名下全部产品与服务状态。

**本阶段不做**：订单、账单、缴费、网络资源、认证计费。

---

## 阶段3：资产台账与电子标签（AMS 基础）

**阶段目标**：资产数字化第一步——建立资产台账、电子标签、生命周期与盘点能力。

**前置依赖**：阶段1。

**需求内容**：
1. 电子标签：为每台光猫、每个分光器端口、每台机柜设备建立唯一电子标签（标签频段按现场选择：[LF / HF / UHF]）；一个资产对应且仅对应一个标签，标签与资产信息可互相查询。
2. 资产档案：记录设备类型、品牌型号、序列号、所属区域/小区/楼栋/弱电井、安装位置、标签编号、标签电量、当前状态、生命周期阶段。
3. 生命周期：资产状态按"入库 → 领用 → 在用 → 故障 → 报废/解绑"顺序流转；状态变更有明确触发条件与操作记录，非法流转被拒绝。
4. 盘点：按区域/小区/楼栋批量盘点，盘点结果与台账自动比对，生成差异清单，差异项可逐条处理（确认 / 修正 / 上报）。

**验收标准**：
- 新建资产后可通过标签编号查询完整档案；
- 资产状态只能按定义顺序流转，非法流转被拒绝并提示原因；
- 盘点差异清单自动生成，差异项可逐条处理。

**本阶段不做**：资产与用户/端口/地址的四码关联（阶段6）、地图展示（阶段8）、扫码强制（阶段6）。

---

## 阶段4：网络资源台账（OSS 基础）

**阶段目标**：建立端口资源台账与状态管理，为资源核查与预占做准备。

**前置依赖**：阶段1。

**需求内容**：
1. 资源台账：OLT、分光器、端口按地址层级（小区/楼栋/弱电井）挂接；端口状态包括空闲 / 预占 / 在用 / 故障。
2. 资源查询：按地址、小区、楼栋、分光器查询端口及其状态。
3. 状态维护：端口故障上报、修复恢复等状态变更有明确流程，变更留痕可追溯。

**验收标准**：
- 端口台账与地址层级一致，端口状态准确；
- 任意小区/楼栋可查其下全部端口与状态；
- 端口状态变更可追溯操作人、时间、原因。

**本阶段不做**：资源核查与预占（阶段5）、配置自动下发（阶段7）、地图（阶段8）。

---

## 阶段5：订单与装维全流程（BOSS 人工闭环）

**阶段目标**：打通装维业务 12 环节的完整闭环骨架（依赖后续阶段的环节暂以人工/简化方式完成），并上线计费、缴费、欠费管理基础。本阶段结束，系统具备"从下单到激活"的完整可用业务。

**前置依赖**：阶段2、3、4。

**需求内容**：
1. 用户下单：客户提交宽带安装/变更订单（产品、地址、联系方式），生成订单。
2. 资源核查：按订单地址查找空闲端口，返回结果（有空闲资源 / 无资源 / 需扩容）；无资源时必须提示可选方案。
3. 端口预占与释放：核查通过后锁定端口；订单取消、超时、失败自动释放端口；同一端口同一时刻只能被一个有效订单占用。
4. 标签预绑定：为订单从阶段3的标签库存中预绑定电子标签，关联订单。
5. 派单：工单分配给装维师傅，包含地址、端口、标签信息。
6. 绑定与激活（人工模式）：装维完成后，人工确认光猫与订单匹配并确认激活；激活结果回传，订单流转为完成状态。
7. 订单跟踪：订单全流程各环节、处理人、耗时实时可查；支持订单取消、变更、退订；任一环节失败不丢数据，可重试或回退。
8. 计费出账：按套餐周期自动生成账单；支持在线缴费（接入支付渠道）、缴费记录、缴费凭证；欠费管理，欠费停机 / 缴费复机规则可配置（本阶段作用于业务侧状态，网络侧生效见阶段7）。

**验收标准**：
- 从下单到激活的完整流程可走通，订单详情可见各环节进度与耗时；
- 并发场景下两个订单不可能占用同一端口；订单取消/失败后端口自动释放；
- 账单与套餐规则一致，可抽查核验；缴费后账单与状态更新；
- 任一环节失败不影响已保存数据，可定位失败环节并重试/回退；
- 订单每个环节均有时间戳与操作留痕。

**本阶段不做**：扫码强制与四码校验（阶段6）、认证计费与配置下发自动化（阶段7）、GIS 更新（阶段8）。

---

## 阶段6：四码合一与扫码强制

**阶段目标**：实现"资产、用户、端口、地址"四码关联与一致性校验，扫码成为绑定、拆机的强制节点。

**前置依赖**：阶段5。

**需求内容**：
1. 四码合一：建立"资产码、用户码、端口码、地址码"四码关联，任一码可反查其他三码及关联关系。
2. 扫码绑定：装维师傅扫码（扫码动作在安装师傅端，服务端负责校验与关联）将实物光猫与订单绑定；绑定结果必须与预绑定一致，不一致时绑定被拒绝。
3. 拆机必扫码：任何拆机操作必须先扫码解绑，未完成扫码不得执行拆机流程，系统强制拦截。
4. 一致性校验：四码关系按配置周期自动核对，发现冲突必须告警并限期处理。

**验收标准**：
- 通过资产码、用户码、端口码、地址码中任意一个可查全四码关系，响应时间 ≤ [X] 秒；
- 扫码与预绑定不一致时绑定被拒绝并提示原因；
- 不扫码直接执行拆机/解绑的操作被系统拦截，无法继续；
- 四码关系每日自动核对，冲突数为 0（或冲突在 [X] 小时内处理完毕）。

**本阶段不做**：认证计费与配置下发自动化（阶段7）、GIS（阶段8）、经营分析（阶段9）。

---

## 阶段7：认证计费与配置自动下发（业网融合）

**阶段目标**：网络侧自动化——用户认证、授权、实时计费（AAA）、OLT 设备管理、配置自动下发；装维现场零手工配置；停机/复机即时生效。

**前置依赖**：阶段5、6。

**需求内容**：
1. 用户认证：用户上线必须通过系统认证（认证标识为 LOID），认证通过后方可上网。
2. 服务授权：认证通过后，按用户订购的带宽模板 / QoS 策略授权网络服务能力。
3. 实时计费：用户上网实时产生话单（时长、流量），话单实时推送至计费系统用于出账，并与阶段5的账单体系衔接。
4. 状态联动：用户停机 / 复机状态即时生效于认证与授权：停机用户即使设备在线也无法获得网络服务，复机后 [X] 分钟内恢复。
5. OLT 设备管理：OLT 及其下挂分光器、端口的台账统一管理（与资产、地址关联）；OLT 在线/离线、光功率、丢包率等指标采集与展示；异常按配置触发告警通知。
6. 配置自动下发：支持配置模板管理、预下发、自动下发；下发成功/失败有明确记录，失败可重试；装维现场无需手工配置光猫数据。
7. 环节自动化升级：将阶段5中人工完成的"创建认证账号、预下发配置、激活用户、激活回调"改为系统自动执行。

**验收标准**：
- 认证成功/失败响应时间 ≤ [X] 毫秒；并发认证能力 ≥ [X] 万用户；
- 话单数据完整率 ≥ [X]%，话单产生到进入计费系统时间 ≤ [X] 分钟；
- 停机用户即使设备在线也无法获得网络服务；复机后 [X] 分钟内恢复；
- 每次配置下发有成功/失败记录，失败可重试并留痕；
- 装维师傅在现场不需要手工录入任何光猫配置数据；
- OLT 状态与告警实时可见，异常按配置触发通知。

**本阶段不做**：GIS（阶段8）、经营分析（阶段9）。

---

## 阶段8：数字孪生 GIS

**阶段目标**：三维地图可视化——八级层级钻取、资产位置展示、资产实时详情、状态变更联动地图。

**前置依赖**：阶段3、4、5、6、7（资产、资源、订单、状态数据齐备）。

**需求内容**：
1. 三维地图底座：基于真实地理信息构建三维地图（引擎在三者中选择：[Cesium / UE5 / Mapbox GL]）。
2. 层级钻取：支持"市级 → 区级 → 街道 → 小区 → 楼栋 → 弱电井 → 分光器 → 端口"八级逐级下钻与逐级回退，每级展示数量统计。
3. 资产可视化：地图上按真实位置展示机柜、弱电井、分光器、光猫及其端口。
4. 资产详情：点击任一资产显示：实时状态（在线 / 离线 / 占用 / 空闲）、光功率、丢包率、关联用户信息、标签电量。
5. 变更联动：资产的新增、拆除、位置变更、状态变化按业务要求的最短周期反映到地图（即 12 环节中的"更新 GIS 地图"）。

**验收标准**：
- 八级层级全部可逐级下钻与回退，每级数量统计准确；
- 点击任意资产，[X] 秒内展示完整详情；
- 地图上的资产状态与业务系统一致，状态变化后 [X] 分钟内反映到地图；
- 大屏/地图加载时间 ≤ [X] 秒。

**本阶段不做**：经营分析（阶段9）。

---

## 阶段9：经营分析

**阶段目标**：经营决策支持——五大核心指标、投资一张图、维护一张表、自动分析报告。

**前置依赖**：阶段1-8（全量业务数据）。

**需求内容**：
1. 核心指标：端口利用率、装机转化率、单用户维护成本、资产健康度评分、区域投资回报率；指标口径可配置，计算规则必须可解释，每个指标可下钻到明细数据。
2. 投资一张图：以热力图形式展示高潜扩容区域，可定位到具体小区/楼栋，展示其端口利用率与投资数据，支撑扩容决策。
3. 维护一张表：自动生成待替换设备优先级清单（按资产健康度、故障率、使用年限排序），排序评分规则公开可解释。
4. 自动报告：按 [日 / 周 / 月 / 季度] 周期自动生成经营分析报告（含指标、图表、结论与建议）并推送。

**验收标准**：
- 五大指标均可核算、可下钻，口径与业务定义一致；
- 热力图定位准确，数据与端口利用率、投资数据一致；
- 维护清单排序可解释，替换建议与资产故障数据一致；
- 报告按设定周期自动生成并推送，生成时间 ≤ [X] 分钟。

**本阶段不做**：新增业务功能（如需新增，进入下一轮迭代规划）。

---

## 附：12 个业务环节与阶段对应关系

| 环节 | 名称（原标识） | 所属阶段 |
|---|---|---|
| 1 | 用户下单（submitOrder） | 阶段5 |
| 2 | 资源核查（checkResource） | 阶段5 |
| 3 | 端口预占（reservePort） | 阶段5 |
| 4 | 合同收费（chargeContract） | 阶段5 |
| 5 | 标签预绑定（applyTag） | 阶段5（预绑定）→ 阶段6（一致性校验） |
| 6 | 创建认证账号（createUserProfile） | 阶段5（人工）→ 阶段7（自动） |
| 7 | 预下发配置（preConfigOLT） | 阶段7 |
| 8 | 派单（dispatchOrder） | 阶段5 |
| 9 | 扫码绑定（scanBind） | 阶段5（人工确认）→ 阶段6（扫码强制） |
| 10 | 激活用户（activateUser） | 阶段5（人工）→ 阶段7（自动） |
| 11 | 激活回调（notifyActivation） | 阶段5 |
| 12 | 更新 GIS 地图（updateMap） | 阶段8 |

## 使用说明

1. 每个阶段可独立提取为一份小需求提示词，按序下发给开发与验收；方括号 [X] 参数沿用主文档口径。
2. 阶段间为强依赖关系：必须按 1→9 顺序实施，不可跳级（每阶段"前置依赖"已注明）。
3. 阶段5 与阶段7 之间保留了"人工 → 自动"的升级路径：阶段5 结束时业务已可用，阶段7 再实现自动化，保证任何阶段完成时系统均可实际使用。
4. 全部 9 阶段完成后，对照主文档《需求提示词-服务端.md》做一次整体回归验收（主文档第九章）。
