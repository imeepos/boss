# 契约文档索引（contract/）

> 单一事实源目录。AI 开发时"只读不猜"的宪法。与他处（需求全案/技术方案/代码/页面草稿）冲突时，以本目录为准；本目录内冲突时，以各自头部标注的「权威源」为准。

| 文档 | 内容 | 解决什么返工 |
|:-----|:-----|:-------------|
| `terms.md` | 订单 12 环节权威清单、状态枚举、关键术语 | 环节数矛盾、字段/枚举方言 |
| `domain-map.md` | 能力域/Agent/internal包/admin页面 四套命名对齐 | 命名体系错位、越界实现 |
| `fields.md` | 页面列名 ↔ 字段名 ↔ 状态枚举 三列对齐 + 命名规则 | 各猜各的字段名、命名风格不统一 |
| `slice-customer-order.md` | 垂直切片（客户→订单）设计：实体/接口/状态机/验收，到接口抽象层 | 各域编码范式不一、越界耦合、状态漂移 |
| `baseline-freeze.md` | Q1 契约基线冻结清单(2026-08-23,commit 188d9d4):12环节/状态/字段/错误码/三端 OpenAPI 规模与冻结规则 | 冻结后契约无序变更、错误码复用 |
| `vendor-adapter-spec.md` | 供应商适配器规范(支付/短信/实名/税务/地图/设备/AAA):接口形状、密钥引用、降级、幂等、沙箱、新增 checklist 与现有对齐评估 | 外部系统接入各自造轮、密钥落库、缺降级路径 |

## 三份事实源的补充说明

本目录仍未覆盖、后续应补的契约文档（按优先级）：

1. `fields.md`（字段字典）：每个域对象的字段名 + 类型 + 枚举，页面列名 ↔ 英文字段 ↔ DB 列三列对照。当前各域 struct 只有 user 域有实体，补齐字段字典可把 struct/migration/页面三处命名锁死。
2. `api-contract.md`（接口契约）：REST/OpenAPI + gRPC proto 的方法、DTO、错误码。当前 `api/openapi/` 和 `api/proto/` 仅 README 占位。

## 编号规范

- 订单环节：12 环节，术语及标识见 `terms.md` 第 1 节。
- 能力域：21 域，简称见 `domain-map.md` 表 1 列"简称"。
- Agent：AG-01~18 + XG-01~04，见全案 7.2。
