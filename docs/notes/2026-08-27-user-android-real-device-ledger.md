# user-android 真机手测台账（2026-08-27 开）

## 真机清单

| serial | 品牌型号 | Android | SDK | 分辨率 | 状态 |
|---|---|---|---|---|---|
| ee9999eb | Xiaomi MI 9 SE (grus) | 11 | 30 | 1080x2340 | USB 在线，可测（APK 未预装，构建安装中） |

## 手测范围（沿用框架修正版）

1. 6 条关键路径深回：登录注册(真码)→产品浏览→下单→订单/账单→我的(实名/地址/券/套餐)
2. 全页面广度走查：功能存在性 + 体验缺陷（返回栈/空态/溢出/间距），按 8~10 页/日
3. 新功能专项：积分、推送（JPush）
4. 权限真机实测：POST_NOTIFICATIONS 弹窗链路、定位前台权限
5. 历史遗留项（复用上一轮记录）：确认变更地址按钮点击随机性；首页进行中订单卡点击区重叠

## 时间窗口（自 08-27 起）

- D0 构建装包 + 环境校验（进行中）
- D1-D3 关键路径深回
- D4-D5 广度走查（89 文件全部页面）
- D6 新功能专项 + 权限实测
- D7-D9 缺陷修复回归 + P0/P1 清零门禁
- 发布前 P0/P1 必须清，否则行使否决权

## 证据

- 证据目录：`docs/notes/user-android-evidence/2026-08-27/`（screencap PNG + uiautomator dump XML，每项断言留文件）
- 断言法：uiautomator dump 取 bounds 断言文案/间距；run-as 读 shared_prefs 断言落库；screencap+PIL 像素断言视觉

## D0 记录（08-27）

- 装包 ✅：debug 包 + `-PbossBaseUrl` 覆盖，安装成功（package:/data/app/.../com.ymm.boss.user）
- MIUI 注入⚠️：`input tap` 偶发 `SecurityException: INJECT_EVENTS`（MIUI USB 调试安全设置保护），重试可过；`keyevent` 不受限；monkey 可注入但坐标随机——真机自动化点击需带重试机制
- **测试侧配置事故（非 App 缺陷）**：首装传 `--base-url http://192.168.0.102:28080`（缺 `/api/user/v1` 前缀），覆盖了 build.gradle.kts debug 默认 `http://192.168.0.102:28080/api/user/v1` → 全接口 404 → 登录页报"接口不存在，请更新 App"（截图 `d0c-sms-fail.png` 归入环境事故证据）。已用正确前缀重装。教训：**--base-url 必须带完整前缀，否则静默 404 且 UI 文案误导为"请更新 App"**
- curl 复核：POST 102 `/api/user/v1/auth/sms-code` = 200 {code:0,ok:true}；无前缀路径 = 404 ✅ 路由正确

## 已确认缺陷

### BUG-01 【P1】底部导航"账单"tab 路由错绑：标签"账单"→ 实际是"我的订单"页
- 等级：P1（导航名实不符，UX×3 零放行类）
- 复现：跨页三态——服务页点"账单"tab(678,2085) → 落"我的订单"页；订单页内点它无变化（已在目标页）；"服务"tab 正常切服务页对照
- 代码：`ui/Nav.kt` TABS 第 3 项 `"orders" to "账单"` + `tabRoute("orders")=Route.Orders`；契约 `docs/user/nav.js` 第 3 tab 为"订单"（orders.html）；App 独立 Route.Bills（我的账单页，从"我的"菜单进，功能完整：当前应缴/立即缴费/余额充值/6期筛选）
- 证据：/tmp/d10-d15、d17 dump；截图 `d2-billtab-deadzone.png`、`d3-my-bills.png`
- 说明：2026-08-27 晚复核——"账单"tab clickable 节点存在（d18b [552,2042]），此前"节点缺失"系 uiautomator dump 帧不全；缺陷机制=**路由错绑**，非点击死区
- 待办：修复二选一——tab 标签改"订单"，或将 tab 路由改 Route.Bills；回归：服务页→账单tab→账单位

## 已验证页面（D0-D1 凌晨，真机 MI 9 SE）

- ✅ 登录（真码链路：获取验证码→查 portal_sms_codes→登录→token 写 secure prefs；首页呈现"下午好，用户"）
- ✅ 首页（功能区 8 入口/账单卡/四 tab/空态"暂无进行中订单"）
- ✅ 订单页（我的订单/全部·进行中·已完成·已取消/暂无订单/没有更多订单了）
- ✅ 服务页（搜索框/宽带·5G·增值服务/家庭宽带100M ¥99/月）
- ✅ 我的页（实名信息-待补登/家庭地址-0个地址/我的套餐/菜单 6 项/手机号脱敏 138****1234）
- ✅ 我的账单 Route.Bills（当前应缴 ¥0.00/立即缴费/余额充值/近 6 期-已缴-未缴/暂无账单）
- ✅ 我的套餐 Route.MyPlan（在网/月费 ¥0/月/合约到期—/安装地址—/账户状态正常）
- ✅ 套餐详情（家庭宽带100M：规格/合约期 12 个月/安装费 ¥0/光猫免费租用/同档可选）
- ✅ 确认订单守卫（无地址→"暂不可提交"+前往地址管理引导文案；"提交订单后请尽快完成支付"提示）
- ✅ 家庭地址管理（空态+新增）+ 地址表单（Barangay 级联/小区/楼栋/单元/联系人/电话留空用账户号）
- ✅ 定位前台权限实测：系统弹窗（拒绝/本次运行允许/仅在使用中允许）→ 授权 granted=true（FINE+COARSE），证据 `d7-location-perm.png`
- ⚠️ 观察：登录页"获取验证码"疑似一次 tap 双触发（UI 报 42300 但库生成码）待复测；首页"未绑定手机号"待产品确认数据态

## 观察项（待复测/待定性）

- OB-02：登录页"获取验证码"疑似单次 tap 双触发 → UI 报 42300 但库已生成码
- OB-03：地址表单空提交无必填校验提示（保存静默无反应，未新增地址）——需排除 input text 注入丢字干扰后复测
- OB-04：在线缴费页"账单加载失败"文案（无账单数据的 failed 态，疑应为"暂无待缴账单"）
- OB-05（测试方法）：`input text` 对字母串注入丢字（"TestVillage01"只剩"V"）——Compose 字段逐字符注入偶发丢字，改用逐字符+间隔或剪贴板注入
- 权限缺口：POST_NOTIFICATIONS 已声明于 Manifest，但 Android 11 无运行时弹窗（13+ API）→ 需 Android 13+ 真机或转代码审查兜底