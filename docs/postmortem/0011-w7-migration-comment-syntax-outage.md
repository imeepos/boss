# 0011 W7 验收迁移注释语法错误致 102 服务中断

- 日期:2026-09-07;作者:W7 执行会话;状态:已修复(9a44ff3c)。
- 影响:102 boss-server 因 000225 迁移失败重启循环约 11 分钟(22:25-22:36 +0800 前后),期间 /api 全量不可用;无数据损坏。
- 时间线:22:23 推 43f74d19(含 000225)→ CI 构建部署 → 22:25 起迁移报 `syntax error at or near 原`(SQLSTATE 42601)重启循环 → 22:34 定位为 000225 up 文件第三行中文注释缺 `--` 前缀 → 22:34 推 9a44ff3c → 22:36 healthz 恢复。
- 根因:迁移 SQL 由会话程序化生成,注释行前缀遗漏;本地仅做 py/go 侧校验,迁移 SQL 未经过任何真实 psql 语法验证即合入 main 并触发自动部署。
- 放弃的方案:回滚镜像(旧镜像 tag 仍在,但部署通道当时由同一故障 runner 承担,来回切换风险更高,选择最短路径前滚修复)。
- 防再犯:
  1. 迁移文件落盘后、提交前,必须经真实 psql 干跑校验(至少 `psql --set ON_ERROR_STOP=1 -f` 于临时库/事务内 ROLLBACK),脚本化验收前置。
  2. 程序化生成 SQL/迁移时,生成器必须断言每行要么以 `--` 开头要么为合法语句起始,禁止裸中文行。
  3. 部署后 healthz+迁移失败告警联动(现有 restart 循环日志可 grep `wiring: app: migrate`),值班巡检纳入。