# 技术栈方案 · 全栈一步到位（兼顾性能）

对应《需求提示词-服务端-9阶段拆分.md》。原则：**一套栈走完 9 个阶段，不需要中途换语言/换框架**；性能热点（AAA 认证、话单、配置下发）从第一天就用高性能组件承载，阶段 7 只是"接入"，不是"重写"。

---

## 一、总体架构

```
┌─ 前端 ─────────────────────────────────────────────┐
│ 管理后台 React+AntD Pro │ 师傅端 H5/小程序 │ GIS 大屏 Cesium │
└──────────────┬─────────────────────────────────────┘
               │ HTTPS / WebSocket
┌──────────────▼─────────────────────────────────────┐
│ API 网关 Kong / APISIX（鉴权、限流、路由）            │
├────────────────────────────────────────────────────┤
│ 业务服务群（Go，模块化单体 → 可拆微服务）              │
│ user │ customer │ asset │ resource │ order │ billing│
├────────────────────────────────────────────────────┤
│ 性能服务群（独立部署，Go/Rust）                       │
│ AAA(RADIUS) │ 话单采集 │ OLT网关(SNMP) │ 配置下发     │
├────────────────────────────────────────────────────┤
│ 编排 Temporal（订单11环节状态机/Saga）                │
├────────────────────────────────────────────────────┤
│ 消息 Kafka │ 缓存 Redis │ 对象存储 MinIO/S3          │
├────────────────────────────────────────────────────┤
│ PostgreSQL(OLTP) │ VictoriaMetrics(时序) │ Doris(OLAP)│
└────────────────────────────────────────────────────┘
```

---

## 二、技术选型明细

### 2.1 后端（主力语言：Go 1.22+）

| 层 | 选型 | 说明 |
|---|---|---|
| Web 框架 | **Go + go-zero 或 Gin** | go-zero 自带 RPC/限流/熔断，直接生产级 |
| 微服务 RPC | **gRPC + Protobuf** | 服务间通信；对外 REST 由网关转 |
| API 网关 | **APISIX** | 动态路由、JWT 鉴权、限流，权限变更即时生效 |
| 工作流/编排 | **Temporal** | 订单 11 环节状态机、失败重试/回退、人工→自动升级只改 Activity 实现 |
| ORM | **GORM / sqlc** | 高并发路径（端口预占）用 sqlc 手写 SQL + `SELECT ... FOR UPDATE` |
| 配置中心 | **Nacos / etcd** | 业务参数（欠费阈值、预占有效期）热更新 |
| 分布式锁 | **Redis + Redisson 语义（redsync）** | 端口预占互斥；DB 唯一约束兜底 |

> 为什么 Go 而不是 Java：单一语言覆盖"业务 + 高性能 AAA + 设备网关"，部署物小、内存低、并发天然强；团队只需掌握一门后端语言。

### 2.2 性能热点专用组件（阶段7 就绪，阶段5 预留接口）

| 组件 | 选型 | 性能指标依据 |
|---|---|---|
| AAA 认证 | **Go 自研 RADIUS（radius 库）或 FreeRADIUS + Lua/Go 插件** | FreeRADIUS 单机 >5 万 pps；自研 Go 可控 LOID 逻辑 |
| 停复机联动 | AAA 订阅 Kafka `user.status.changed` 主题 | 复机 ≤ 分钟级（写 Redis 授权缓存 TTL 失效即可秒级） |
| 话单 | **goRTR→Kafka→Flink→Doris** | 实时话单完整率 ≥99.99%，端到端秒级 |
| OLT 采集 | **SNMP（gosnmp）轮询 + Trap 接收** | 光功率/丢包率写 VictoriaMetrics |
| 配置下发 | **Kafka 异步 + 重试队列** | 模板渲染（text/template）→ 网关下发 → 成功/失败留痕 |
| 设备协议 | SNMP / TR-069(阶段可延后) / NETCONF | 分光器/光猫以 LOID + SNMP 为准 |

### 2.3 数据层

| 存储 | 选型 | 用途 |
|---|---|---|
| OLTP | **PostgreSQL 16** | 全部业务台账；地址层级用 `ltree`；审计表按月分区；四码关联表加四方向索引 |
| 缓存 | **Redis Cluster** | 会话、权限快照、端口状态缓存、预占锁 |
| 消息 | **Kafka** | 话单、状态变更事件、GIS 联动事件、配置下发任务 |
| 时序 | **VictoriaMetrics** | OLT 指标、光功率历史 |
| OLAP | **Apache Doris** | 阶段8/9：指标下钻、热力图聚合、报告 |
| 对象 | **MinIO** | 缴费凭证、报告 PDF、盘点导入文件 |

### 2.4 前端

| 端 | 选型 | 说明 |
|---|---|---|
| 管理后台 | **React 18 + TypeScript + Ant Design Pro + Vite** | 7 角色权限由后端 RBAC 接口驱动菜单 |
| 师傅端 | **uni-app（H5 + 微信小程序）** | 一套代码，扫码调原生相机 |
| GIS 大屏 | **React + Cesium + Cesium ion 自托管 3D Tiles** | 八级钻取；数据量>万级用 **PrimCAD/3D Tiles + GeoServer** 瓦片 |
| 图表 | **ECharts** | 热力图、指标下钻、报告图表 |
| 实时推送 | **WebSocket（中心化网关 SSE/WS）** | 订单进度、告警、地图状态联动 |

### 2.5 可观测与运维

- 日志：**Loki + Promtail**（轻量）或 ELK
- 指标/告警：**Prometheus + Alertmanager**（接短信/钉钉/企微）
- 链路：**OpenTelemetry + Jaeger**
- 部署：**Docker + Kubernetes + Helm**；CI/CD 用 GitLab CI / GitHub Actions
- 数据库迁移：**golang-migrate**；压测：**k6**

---

## 三、关键设计决策（对应验收项）

### 3.1 端口预占互斥（阶段5 验收："两订单不可能占同端口"）
```sql
-- 唯一约束兜底 + 事务行锁
UPDATE port SET status='RESERVED', order_id=$1
WHERE id=$2 AND status='IDLE';  -- 影响行数=0 即抢占失败
```
+ Redis 锁削峰 + 定时任务扫描超时释放（预占有效期走配置中心）。

### 3.2 状态机（资产生命周期、订单 11 环节）
- 定义驱动的状态机表（state, event, next_state, guard），非法流转直接拒绝；
- Temporal Workflow 编排，每环节 Activity 有时间戳落审计表，失败可重试/补偿。

### 3.3 四码合一（阶段6）
独立关联表 `quad_link(asset_id, user_id, port_id, addr_id)`，四列各建索引 + 唯一约束；任一码反查走单表索引，毫秒级。每日对账任务 = Flink/SQL 定时核对 + 冲突告警。

### 3.4 停复机即时生效（阶段7）
授权决策放 Redis（key=user LOID，TTL 60s）；停机 → 删 key + 推 AAA → 秒级生效；复机同理。

### 3.5 GIS 联动（阶段8）
业务事件 → Kafka `asset.changed` → GIS 服务消费 → 更新 Cesium Entity/3D Tiles 元数据；大屏 WS 推送增量，不整图刷新。

### 3.6 指标可解释（阶段9）
指标口径配置化存 PostgreSQL，计算 SQL 模板化；Doris 建宽表 `dws_asset_port_day`，下钻 = 维度上卷，天然可解释。

---

## 四、阶段落地映射（不换栈，只加部署）

| 阶段 | 新增内容 | 复用 |
|---|---|---|
| 1 | user 服务、审计、Nacos、PG | — |
| 2 | customer/product 服务 | 全部 |
| 3 | asset 服务、MinIO（导入） | 全部 |
| 4 | resource 服务 | 全部 |
| 5 | order 服务 + **Temporal** + billing + Kafka | 全部 |
| 6 | quad_link、扫码 RPC、对账任务 | 全部 |
| 7 | **AAA 独立部署 + VM + 配置下发服务**（接口阶段5已留） | 全部 |
| 8 | GIS 服务 + Cesium 前端 | 全部 |
| 9 | **Doris + Flink** + 报告服务 | 全部 |

---

## 五、最低硬件起步（可随阶段扩容）

| 组件 | 起步规格 |
|---|---|
| K8s 集群 | 3 节点（8C16G） |
| PostgreSQL | 主从 2 台（8C32G + SSD） |
| Redis | 3 主 3 从（4C8G） |
| Kafka | 3 节点（4C16G） |
| VictoriaMetrics / Doris | 单节点起步（8C32G） |

---

## 六、风险与备选

1. **Temporal 运维成本** → 备选：自研状态机 + outbox 表（功能降级但零额外组件）
2. **Cesium 三维数据生产** → 楼栋/弱电井可用 2.5D 倾斜摄影或白模起步，逐步替换
3. **FreeRADIUS vs 自研 Go AAA** → 先 FreeRADIUS 插件快速达标，LOID 逻辑复杂后再迁 Go
4. **Doris 引入太晚** → 阶段5 起话单就落 Kafka，Doris 只需接管道，不需要回补历史
