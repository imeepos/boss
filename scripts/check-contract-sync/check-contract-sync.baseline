# check-contract-sync baseline:已知且有意接受的差异。
# 规则:新增差异禁止进此文件;只有"存量债务 + 明确整改意图"才允许登记。
# 每条必须有登记理由与整改方向。

# ---- C. 超 300 行存量(08-18 体检发现,列入整改清单) ----
# addresses 系列已拆 http_address.go,http_org.go 豁免已移除(2026-08-18)

# ---- A. 路由参数命名历史差异 ----
# 2026-09-07:route:/products/{id} 历史条目移除——该键缺 face 前缀从未生效;
# 同日 A2 假阳性链查实后,契约删除幻影 /products/{productId} 块(product.yaml
# getProduct/string 版,无实现且 schema 与实现不符),实现 :id 与存留契约
# /products/{id} 一致,本段不再有差异。

# ---- D. 迁移编号历史撞号(D 项门禁上线时体检发现,2026-08-22) ----
# migrator 以完整文件名为版本键,两者均已应用、功能正常;编号唯一惯例自此门禁起
# 强制,历史不改名(改名需同步 schema_migrations,风险大于收益)。
mig:000044
mig:000095

# ---- E. menu.def 权限码复用豁免(检查 E:menuperm.go) ----
# capacity(容量视图,P5-W1)复用端口台账权限码 menu:resource——任务书裁定
# 「菜单注册与权限码对齐 menu:resource」,不新增权限码;前端 MenuItem.perm='resource',
# 越权判定/自定义角色推导均按 menu:resource 判定(role-menu.ts perm 覆盖)。
menu:capacity

# aaasession(A6 在线会话页,2026-09-06 登记)复用 LOID 台账权限码 menu:loaccount——
# /aaa/sessions 路由族 permCode 本就是 menu:loaccount(routes_gen),页面 MenuItem.perm='loaccount',
# 不新增权限码;若未来需要独立 aaasession 权限码,再走新迁移登记 permissions+role_permissions。
menu:aaasession

# ---- A2. 方法级对账存量:2026-09-07 首扫登记 6 条,当日全部清零 ----
# geo names ×2 / stripe done,cancel ×2:系扫描器与提取器盲区(引号路径键、
# for-range 字面量循环注册,见 alignment-audit.md §13 A5/A6),修复后假阳性
# 自然消失,豁免条目移除;
# products ×1:契约幻影块(见 §13 A7)删除后消除。
# 豁免规则不变:新增差异必须凭「存量债务 + 整改方向」登记。
