# 后端经验索引

> Go 服务端、数据库、迁移、API、pgx、契约模型。开工前先扫一遍标题。

---

## 1. Go / pgx / 数据库

| # | 来源 | 要点 |
|---|------|------|
| 1 | lessons #19 | Go 接口加方法时同一提交补齐测试桩，编译报错清单就是桩清单 |
| 2 | lessons #22 | sysadmin 403：先查远端库 `schema_migrations` 最大版本 vs `ls migrations/*.up.sql`——权限来自 role_permissions 显式行，迁移漏跑是首要嫌疑 |
| 3 | lessons #23 | 42200 参数非法：先读后端请求 struct（如 geo.CountryAttrs）再拼 JSON，不凭直觉猜字段类型 |
| 4 | lessons #24 | 本机无 psql：`/tmp` 临时 go 程序 + pgx 直连 DSN（configs/config.example.yaml 有现成连接串） |
| 5 | lessons #25 | 新增迁移：先 `ls migrations/*.up.sql \| tail -5` 看真实最大编号，代码注释里的迁移号也要 grep 交叉验证 |
| 6 | lessons #39 | pgx 占位符从 `$1` 连续编号，报 42P18 先查编号再查 `::text` 类型标注 |
| 7 | lessons #57 | pgx 严格参数校验：占位符少于传入参数直接报 `unused argument`，每条语句只喂自己的参数 |
| 8 | lessons #58 | t.Cleanup 里用连接池时，资源释放同走 t.Cleanup（LIFO），不能 defer 与 t.Cleanup 混用 |
| 9 | lessons #59 | 测试数据清理按"测试专用命名模式"(`^(e2e|w8)[0-9]+$`)匹配，不信精确后缀——子测试常自造独立后缀 |
| 10 | lessons #61 | 垃圾数据溯源：按 `split_part(path,'.',1)` 分组 + created_at 日期，前缀对 grep 测试文件 |
| 11 | lessons #62 | pgx 可空列缺 COALESCE → `Scan(&int16)` 报错 → 500，排查 Go API 500 先看 nullable 列有没有 COALESCE |
| 12 | known-issues #8 | sysadmin 403 迁移漏跑：`HasPermission` 只查 role_permissions 显式行，sysadmin 无隐式全权 |
| 13 | known-issues #9 | pgx 读 schema_migrations.version 是 TEXT（如 '000037_alarm_retest'），不是 int |
| 14 | known-issues #15 | app.env 超管口令对既有 admin 不生效（ON CONFLICT DO NOTHING），直接 UPDATE password_hash 对齐 |
| 15 | known-issues #18 | 共享库被集成测试污染：三个测试已补自清理，入口都是 `BOSS_PG_TEST_DSN` 指向了共享库 |
| 16 | known-issues #19 | pgx 可空列缺 COALESCE 导致 500 内部错误，排查 Go API 500 第一步 |
| 17 | red-lines #3 | 禁止用假数据/mock 替代真实后端做开发验证，dev 免登录用真实 /auth/login 换 JWT |
| 18 | techniques #7 | 后端 API 冒烟：vite 代理转发到 102 部署，curl 走 `localhost:5173/api/v1/...` 测真实链路 |
| 19 | techniques #8 | 手工补迁移：pgx 整文件 Exec（含 BEGIN/COMMIT），成功后 INSERT schema_migrations 记录版本 |
| 20 | techniques #18 | 迁移回环验证：单事务 down→up，剥离文件内 BEGIN/COMMIT，外层起事务跑完两个文件再 commit |
| 21 | techniques #19 | 真实数据三层校验：结构完整性→总数对官方口径→抽查易错点 |
| 22 | techniques #20 | FK 依赖查询：`pg_constraint` 按引用链排删除序 |
| 23 | techniques #22 | 测试自清理验证：`BOSS_PG_TEST_DSN` 指真库跑测试 + psql 按前缀计数残留为 0 |
| 24 | techniques #23 | 垃圾数据溯源：按前缀分组 + created_at 对 grep 测试文件，十分钟锁定污染源 |

## 2. 契约 / 模型

| # | 来源 | 要点 |
|---|------|------|
| 1 | lessons #20 | 超管初始化：走「启动引导 + ON CONFLICT DO NOTHING」，密码只从环境变量注入 |
| 2 | lessons #38 | app.env 口令对既有账号不生效：查 created_at/real_name 判断历史遗留，直接 UPDATE 哈希对齐 |
| 3 | techniques #9 | 超管部署：`BOSS_ADMIN_USERNAME` + `BOSS_ADMIN_PASSWORD` 经 bootstrap 幂等创建，已存在静默跳过 |

## 3. 开工前 grep 关键词

写后端代码前，用这些关键词检索 lessons.md + known-issues.md + red-lines.md：

```
pgx, Scan, COALESCE, 迁移, 权限, 403, 500, nullable, gc_args, cleanup, 测试, 垃圾, DSN, 直连, 占位符, 类型, 契约, 字段, 枚举, 超管, bootstrap
```