paths:
  /quad-links:
    get:
      tags: [Quad]
      summary: 四码合一关联查询(资产/客户/端口/地址任一码反查,见 docs/contract/fields.md 5.1)
      operationId: queryQuadLinks
      parameters:
        - name: code
          in: query
          required: true
          schema: { type: string }
          description: 任一码(资产 EPC/用户/端口/地址码)
      responses:
        '200':
          description: 四码关联关系
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      type: object
                      properties:
                        assetCode: { type: string }
                        customerCode: { type: string, description: 客户域编码,取值为客户认证账号 LOID(与 worker 端一致) }
                        portCode: { type: string }
                        addrCode: { type: string }
                        status: { type: string, enum: [LINKED, CONFLICT, UNLINKED] }

  /quad-conflicts:
    get:
      tags: [Quad]
      summary: 四码对账冲突/告警列表
      operationId: listQuadConflicts
      responses:
        '200': { $ref: './schemas.yaml#/components/responses/List' }

  /quad-conflicts/{conflictNo}/resolve:
    post:
      tags: [Quad]
      summary: 冲突处理
      operationId: resolveQuadConflict
      parameters:
        - { name: conflictNo, in: path, required: true, schema: { type: string } }
      responses:
        '200': { $ref: './schemas.yaml#/components/responses/Ok' }

  /scan-logs:
    get:
      tags: [Quad]
      summary: 扫码绑定记录(订单第 9 环节,预绑定 vs 现场扫码校验)
      operationId: listScanLogs
      parameters:
        - $ref: './schemas.yaml#/components/parameters/keyword'
      responses:
        '200': { $ref: './schemas.yaml#/components/responses/List' }
