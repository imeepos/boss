# 服务端系统需求提示词（BOSS + OSS + AMS + BSS + AAA + OLT + GIS）

## 使用说明

1. 本提示词是服务端系统的完整业务需求，可直接复制使用；方括号 [ ] 内为待确认参数，填入实际数值后使用，不要删除方括号结构。
2. 本提示词只描述"系统必须做什么"与"如何验收"，不包含技术方案、技术选型、架构设计。
3. 文中所有"必须"条目均为验收项，交付时逐条核验。
4. 其余三个端（安装师傅端、用户端、管理端）的需求提示词单独编写，待本提示词确认后继续。

---

## 一、任务指令（作为提示词开头使用）

你是一名资深宽带运营商业务系统需求分析师。请严格基于以下"需求说明"，输出服务端系统的完整需求规格书。要求：

- 完整覆盖下列全部需求条目，不得省略、不得臆造；
- 为每条需求给出明确、可测量的验收标准；
- 只描述业务需求与验收标准，禁止输出技术方案、技术选型、接口路径、架构设计、代码。

---

## 二、业务背景与目标

本单位为 [某省级运营商 / 广电网络 / 政企专网运营商]，现面临以下核心痛点：

1. 资产台账不准：光猫、分光器等设备采用纸质标签管理，盘点效率低，账实不符率约 [X]%。
2. 资源数据不通：BOSS 系统的用户数据与 OSS 系统的网络端口数据长期割裂，经常出现"已缴费用户无端口可用"的情况。
3. 装维效率低：师傅上门需手工配置光猫数据，平均耗时 [X] 分钟/单，且易出错。
4. 经营分析粗放：无法精准核算每个分光器端口的投资回报率，扩容决策凭经验。

系统建设目标（总体验收依据）：

1. 资产账实相符率达到 [X]% 以上，盘点实现电子化；
2. 用户数据与端口数据实时联动，彻底消除"已缴费用户无端口可用"；
3. 装维单均耗时降至 [X] 分钟以内，装维现场零手工配置；
4. 经营指标可按分光器端口粒度核算，扩容决策有数据支撑。

---

## 三、服务端能力域范围

服务端由以下能力域组成，各能力域职责如下（详细需求见后文对应章节）：

1. AMS 资产管理：资产台账、电子标签、资产生命周期、盘点。
2. GIS 地理信息：三维地图底座、资产位置展示、层级钻取、资产实时状态。
3. BSS 业务支撑：客户档案、产品与资费、计费出账、支付缴费、账务管理。
4. BOSS 综合运营：订单全流程管理、业网协同规则、订单与工单处理。
5. OSS 网络资源：端口资源核查、端口预占与释放、设备配置自动下发。
6. AAA 认证授权计费：用户认证、服务授权、实时计费。
7. OLT 设备管理：OLT 及下挂设备台账、状态监控、配置管理。

---

## 四、用户角色与权限需求

服务端必须支持以下角色，每个角色只能访问其职责范围内的功能与数据：

1. 客户（个人用户）：查询本人订单、账单、缴费记录、报障记录、资费与在线状态（通过用户端访问）。
2. 装维师傅：领取/接收工单、扫码绑定资产、上报装维结果、异常上报（通过安装师傅端访问）。
3. 资产管理员：资产台账维护、标签管理、盘点、资产状态变更。
4. 资源运维管理员：端口资源管理、OLT 设备管理、GIS 数据维护、网络拓扑维护。
5. 业务运营/客服人员：订单处理、客户档案查询、工单与投诉处理。
6. 经营分析人员：指标看板、报表查询、报告生成。
7. 系统管理员：账号、角色、权限、业务参数、审计日志管理。

**验收标准：**

- 每个角色登录后仅可见其权限范围内的功能与数据；
- 权限变更即时生效，无需重新部署；
- 所有关键操作（订单变更、资产变更、资源变更、配置下发、权限变更）可追溯操作人、操作时间、操作内容。

---

## 五、各能力域功能需求

### 5.1 资产管理（AMS）

业务要求：

1. 电子标签管理：为每台光猫、每个分光器端口、每台机柜设备建立唯一电子标签（标签频段按现场选择：[LF / HF / UHF]）。一个资产对应且仅对应一个标签，标签与资产信息必须可互相查询。
2. 资产档案：每个资产必须记录：设备类型、品牌型号、序列号、所属区域/小区/楼栋/弱电井、安装位置、所在端口、绑定用户、标签编号、标签电量、当前状态、生命周期阶段。
3. 四码合一：必须实现"资产码、用户码、端口码、地址码"四码关联，任一码可反查其他三码及其关联关系。
4. 生命周期管理：资产状态按"入库 → 领用 → 在用 → 故障 → 报废/解绑"顺序流转。状态变更必须有明确的触发条件与操作记录。"拆机必扫码"为强制节点：任何拆机操作必须先扫码解绑，未完成扫码不得执行拆机流程。
5. 盘点：支持按区域/小区/楼栋批量盘点，盘点结果与台账自动比对，生成差异清单。

**验收标准：**

- 新建资产后可通过标签编号查询完整档案，扫描标签可显示全部关联信息；
- 通过资产码、用户码、端口码、地址码中任意一个，可查全四码关系，查询响应时间 ≤ [X] 秒；
- 资产状态只能按定义顺序流转，非法流转被拒绝并提示原因；
- 不扫码直接执行拆机/解绑的操作必须被系统拦截，无法继续；
- 盘点差异清单自动生成，每条差异项可逐条处理（确认 / 修正 / 上报）。

### 5.2 数字孪生 GIS

业务要求：

1. 三维地图底座：基于真实地理信息构建三维地图（引擎在三者中选择：[Cesium / UE5 / Mapbox GL]）。
2. 层级钻取：必须支持"市级 → 区级 → 街道 → 小区 → 楼栋 → 弱电井 → 分光器 → 端口"八级逐级下钻与逐级回退，每级展示数量统计。
3. 资产可视化：地图上按真实位置展示机柜、弱电井、分光器、光猫及其端口。
4. 资产详情：点击任一资产必须显示：实时状态（在线 / 离线 / 占用 / 空闲）、光功率、丢包率、关联用户信息、标签电量。
5. 变更联动：资产的新增、拆除、位置变更、状态变化必须按业务要求的最短周期反映到地图。

**验收标准：**

- 八级层级全部可逐级下钻与回退，每级数量统计准确；
- 点击任意资产，[X] 秒内展示完整详情；
- 地图上的资产状态与业务系统一致，状态变化后 [X] 分钟内反映到地图；
- 大屏/地图加载时间 ≤ [X] 秒。

### 5.3 业务支撑（BSS）与综合运营（BOSS）

业务要求：

1. 产品与资费：支持宽带产品、带宽套餐（如 [X]M / [X]M）、增值服务的配置与发布；资费规则可配置、可调整。
2. 客户档案：客户注册、实名信息、家庭地址、历史订单、账单、缴费记录、服务记录统一管理，一人一档。
3. 订单管理：客户下单后生成订单，订单全流程（从下单到激活完成）可跟踪；任何时刻可查询订单当前环节、处理人、各环节耗时；支持订单取消、变更、退订。
4. 计费出账：按套餐与用量规则自动生成账单；支持欠费管理，欠费停机 / 缴费复机规则可配置。
5. 支付网关：支持在线缴费（接入支付渠道）、缴费记录、缴费凭证管理。
6. 账务联动：缴费、欠费状态变化必须实时同步到网络服务状态（欠费停机、缴费复机）。
7. 数据一致性：BSS 与 BOSS 必须共享同一套客户、订单、账单数据，不得出现两套不一致的数据。

**验收标准：**

- 订单从创建到完成，每个环节均有时间戳与操作留痕；
- 账单生成与套餐规则一致，抽查核验准确率 ≥ [X]%；
- 缴费成功后 [X] 分钟内完成复机；欠费达到阈值 [X] 天后自动停机；
- 客户档案可查询其名下全部订单、账单、缴费记录、资产与端口；
- BSS 与 BOSS 查询同一客户/订单/账单，结果完全一致。

### 5.4 网络资源（OSS）

业务要求：

1. 资源核查：用户下单时，系统必须在目标地址所属区域/分光器下查找空闲端口并返回核查结果（有空闲资源 / 无资源 / 需扩容）；无资源时必须给出可选方案提示。
2. 端口预占与释放：资源核查通过后必须锁定（预占）该端口，防止重复分配；订单取消、超时或失败必须自动释放端口。同一端口同一时刻只能被一个有效订单占用。
3. 配置自动下发：装维完成后，系统必须按预配置规则自动完成网络设备配置，装维现场无需手工配置光猫数据。
4. 资源台账：端口状态（空闲 / 预占 / 在用 / 故障）实时准确，与订单、资产、用户数据联动。

**验收标准：**

- 资源核查结果与端口真实状态一致，误判率 ≤ [X]%；
- 并发场景下两个订单不可能占用同一端口（并发测试通过）；
- 订单取消/失败后端口自动释放，释放时长 ≤ [X] 分钟；
- 装维师傅在现场不需要手工录入任何光猫配置数据。

### 5.5 认证、授权与计费（AAA）

业务要求：

1. 用户认证：用户上线必须通过系统认证（认证标识为 LOID），认证通过后方可上网。
2. 授权：认证通过后，按用户订购的带宽模板 / QoS 策略授权网络服务能力。
3. 实时计费：用户上网实时产生话单（时长、流量），话单实时推送至计费系统用于出账。
4. 状态联动：用户停机 / 复机状态必须即时生效于认证与授权（停机用户无法获得服务，复机后恢复）。

**验收标准：**

- 认证成功/失败响应时间 ≤ [X] 毫秒；
- 并发认证能力 ≥ [X] 万用户；
- 话单数据完整率 ≥ [X]%，话单产生到进入计费系统时间 ≤ [X] 分钟；
- 停机用户即使设备在线也无法获得网络服务；复机后 [X] 分钟内恢复服务。

### 5.6 OLT 设备管理

业务要求：

1. 设备档案：OLT 及其下挂分光器、端口的台账统一管理，与 GIS 位置、资产信息关联。
2. 状态监控：OLT 在线/离线状态、光功率、丢包率等指标采集与展示；异常时按配置通知运维。
3. 配置管理：支持配置模板管理、预下发、自动下发，下发成功/失败有明确记录并可重试。

**验收标准：**

- OLT 状态与告警实时可见，异常按配置触发通知；
- 每次配置下发有成功/失败记录，失败可重试并留痕；
- 设备档案与资产管理、GIS 数据一致。

### 5.7 经营分析

业务要求：

1. 核心指标：端口利用率、装机转化率、单用户维护成本、资产健康度评分、区域投资回报率。指标口径可配置，计算规则必须可解释。
2. 投资一张图：以热力图形式展示高潜扩容区域，支撑扩容决策。
3. 维护一张表：自动生成待替换设备优先级清单（按资产健康度、故障率、使用年限排序）。
4. 自动报告：按 [日 / 周 / 月 / 季度] 周期自动生成经营分析报告（含指标、图表、结论与建议）。

**验收标准：**

- 每个指标均可下钻到明细数据，口径与业务定义一致；
- 热力图可定位到具体小区/楼栋，展示其端口利用率与投资数据；
- 维护清单的排序评分规则公开可解释，替换建议与资产故障数据一致；
- 报告按设定周期自动生成并推送，生成时间 ≤ [X] 分钟。

---

## 六、核心业务闭环（装维全流程 11 个业务环节）

服务端必须支撑以下 11 个业务环节（名称后括号内为原需求中的环节标识）构成完整闭环。环节顺序固定，每个环节有明确的触发条件、执行方、成功/失败判定，支持失败重试与回退：

1. 用户下单（submitOrder）：用户提交宽带安装/变更订单，含产品、地址、联系方式。
2. 资源核查（checkResource）：系统在目标地址所属区域查找空闲端口并返回核查结果；无资源时提示可选方案。
3. 端口预占（reservePort）：核查通过后锁定端口，进入保留状态。
4. 标签预绑定（applyTag）：为本次装维预绑定电子标签，关联端口、用户、地址。
5. 创建认证账号（createUserProfile）：为用户创建认证/计费账号。
6. 预下发配置（preConfigOLT）：预生成光猫所需配置，装维现场无需手工配置。
7. 派单（dispatchOrder）：将工单分配给装维师傅，包含地址、端口、标签、配置信息。
8. 扫码绑定（scanBind）：装维师傅现场扫码，将实物光猫与系统绑定（必须与预绑定一致）。
9. 激活用户（activateUser）：绑定完成后激活用户服务。
10. 激活回调（notifyActivation）：激活结果回传业务系统，订单流转为完成状态。
11. 更新 GIS 地图（updateMap）：资产位置、状态变化同步至 GIS。

**验收标准（闭环）：**

- 任一环节失败不影响已保存数据，可定位失败环节并重试或回退；
- 订单详情可见 11 个环节的实时进度与耗时；
- 从下单到激活完成的全流程时长 ≤ [X] 天（或 [X] 小时）；
- 11 个环节全部有操作留痕，可审计。

---

## 七、数据与一致性需求

1. 业网数据一致：用户数据（BSS/BOSS）与端口数据（OSS）必须实时联动，任一数据变更必须同步；"已缴费用户无端口可用"为禁止出现的状态。
2. 四码一致：资产、用户、端口、地址四码关系必须一致，发现冲突必须告警并限期处理。
3. 历史可追溯：订单、资产、端口、配置、话单等关键业务数据的变更全部留痕，可审计。
4. 存量兼容：必须兼容现有 BOSS/OSS 系统的数据格式（现有系统厂商：[华为 / 中兴 / 亚信 / 自研]），存量数据可导入、可核对、可纠错。

**验收标准：**

- 模拟"已缴费用户"业务场景：任何时刻该用户要么有可用端口，要么处于明确的"待资源"状态，不允许出现无端口可用且无提示的情况；
- 四码关系每日自动核对，冲突数为 0（或冲突在 [X] 小时内处理完毕）；
- 存量数据导入完成后账实相符率 ≥ [X]%，差异项可逐条核对修正。

---

## 八、非功能性需求与约束

1. 性能：支持 [X] 万用户并发认证；核心业务响应时间 < [X] 毫秒；大屏/地图加载 < [X] 秒；批量操作（盘点、导入）在 [X] 万级数据量下可用。
2. 可用性：核心业务（下单、认证、计费）全年可用率 ≥ [X]%；故障恢复时间 ≤ [X] 分钟。
3. 安全合规：满足 [等保三级 / 网络安全法 / 行业合规要求]；用户隐私数据加密存储；操作审计完整。
4. 环境适应性：电子标签须适应 [金属机柜 / 户外防水 / 高温] 等现场环境，标签选型与测试结果作为验收项。
5. 交付节奏：整体开发周期 ≤ [X] 个月，分 [X] 期上线，每期有明确的可验收范围。
6. 需求参照基准：功能完整性参照华为 iManager NCE、科信智能 ODN、安徽电信数字孪生平台（年化效益 1.35 亿）；计费与运营管理能力参照 3GPP TS 32.240（计费架构）、ITU-T M.3050（电信运营地图）。

**验收标准：**

- 性能指标按上述数值实测通过；
- 通过 [等保三级 / 合规] 测评（或具备测评条件）；
- 每期上线提交当期验收清单，验收通过后方进入下一期。

---

## 九、总体验收标准（汇总）

1. 资产账实相符率 ≥ [X]%，盘点全程电子化，拆机未扫码被拦截（对应目标 1、5.1）。
2. 任意"已缴费用户"均有端口可用或处于明确待资源状态，四码关系冲突为 0（对应目标 2、5.4、七）。
3. 装维现场零手工配置，单均耗时 ≤ [X] 分钟，11 环节全流程可跟踪（对应目标 3、六）。
4. 五大经营指标可核算、可下钻，热力图与维护清单可用，报告按周期自动生成（对应目标 4、5.7）。
5. 三类前端（安装师傅端、用户端、管理端）所需全部服务能力均可由服务端提供（见四、五、六章），并发与性能指标实测达标（对应八）。
